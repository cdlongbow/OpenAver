# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [0.15.3] - 2026-08-29

> 這一版只影響 **DMM 這一個來源**：它過去要靠你自己一片一片查才會慢慢「學會」怎麼找片，而**它學錯的時候你不會知道**。現在改成隨版本附一張查過的對照表，裝好就是完整的。你的影片檔與資料庫一個位元組都沒動。

### Added
#### 📖 DMM 的番號對照表改成隨版本附上，不用自己養

- DMM 站內用的是它自己的一套編號（`STARS-627` 在它那裡叫 `1stars00627`），中間那段前綴每個片商都不一樣。過去這張對照表是**你這台電腦自己累積出來的**：查過一次才學得到一條，**換電腦、重裝、或清掉資料夾就全部歸零**。
- 現在改成**跟著版本一起出貨**（164 條）。新裝的人第一次用就是完整的，不需要先「養」它。
- 這張表不是猜的：拿 DMM 自己的 335 組上架資料離線對帳，**組出來的編號有 320 筆逐字命中**（那證明的是「編號怎麼組」這條算式對得起真實資料）。少數無法從那份資料直接佐證的合輯系列，已在出貨前逐條**真連線**驗過。

### Fixed
#### 🩹 你本機那張自己學出來的表，有一部分是錯的——這一版起不再使用它

- 全部查證之後發現：本機自學值裡 **53 條有 21 條是錯的**。用到錯的那條時，DMM 會回**別部片**的資料——而它看起來完全正常，**你不會收到任何警告**，錯的標題、錯的女優就這樣寫進 NFO 和封面。
- 這一版起 DMM **完全不再讀取**本機那兩個檔（`dmm_prefix_hints.json` / `dmm_content_ids.json`）。**檔案本身不刪，只是不再用**——你想留著或自己刪掉都可以。
- **會不會因此少查到片？實測不會。** 本機那批值裡確認正確的四條（`STARS` / `SDDM` / `IE` / `ZZZA`）已經逐條驗過並收進出貨表；停用整批本機值之後，四支真連線測試照樣全部通過。

#### 🔍 算不出來的時候多試兩條路，`NWF-237` 這類片查得到了

- 過去對照表算不出來時就直接放棄。現在多兩條補償路徑：一是改用 DMM 搜尋看得懂的查詢寫法（過去送 `STARS525` 回 0 筆，改送 `STARS 525` 就找得到）；二是改用「不補零」的另一種編號寫法再試一次。
- 兩條路都會**核對回來的番號是不是你要的那一部**才採用，不會為了「有結果」而拿錯的片給你。

#### 🌐 DMM 的「直連」現在尊重你的系統代理設定

- 過去 DMM 是全部八個來源裡**唯一**硬性繞過系統代理的那一個。你在系統裡設了代理、其他來源都照走，只有 DMM 自己跑掉。
- 現在它跟其他來源一致（也跟 javdb 在 0.15.1 的改動一致）。**設定頁不需要任何改動**，「直連」的字面意思沒變——它描述的一直是你的處境，不是我們的傳輸方式。

### 已知限制
- **這張表涵蓋的是近期的片商前綴。** 很舊的片（例如 2012 年的 `MIDD-150`）DMM 那邊本來就查不到，這一版不會改變這件事——但 javbus／javdb 照樣有，**你只是少了 DMM 那一份欄位，不是查不到那部片**。
- **有些片 DMM 只上架了分集版本，這一版起不再拿分集的資料充當整片。** 例如 `HUST-082`，DMM 上只有 `HUST-082-02` 這種分集條目——過去 OpenAver 會把分集的標題與封面直接當成整片給你（那份資料其實屬於其中一段），現在會判定「這不是你要的那一部」而回無結果，**改由 javbus／javdb 補上**。你少的是 DMM 那一份欄位，不是整部片查不到。
- 表會隨時間變舊（新片商、新前綴）。這一版附了一支開發用的體檢腳本可以量它老了多少，但**那是開發流程的東西，不在你的畫面上**。

測試數 7408 → 7485（pytest，本版新增 77 支；npm test 維持 1330——本版是純後端改動，前端零檔案變動）。

## [0.15.2] - 2026-08-28

> 這一版全部在**瀏覽頁**：切換封面／海報的動畫終於播得出來、開著不動作時畫面真的會停下來、右上角那個四選一的下拉換成一顆按鈕。**有一項使用者可見的變更**——表格與清單兩種呈現預設不再顯示，要到設定裡開（見 Changed）。你的資料一個位元組都沒動。

### Added
#### 🎛 右上角改成一顆鈕，點一下就換
- 過去要先點開下拉、在四個選項裡挑。現在是**一顆按鈕**，圖示顯示「現在是什麼」，點一下就在**完整封面 ↔ 直式海報**之間切換。鍵盤 `A` 鍵也跟著只在這兩段循環。
- **手機／窄視窗看不到這顆鈕**（按 `A` 也不會有反應）。原因是窄視窗本來就一律用直式裁切，那顆鈕按下去畫面**一點都不會變**，卻會靜默改掉你在桌機選的卡型。

#### ⚙ 設定裡多一顆「顯示表格與清單」開關
- 位置在「設定」→「列表顯示預設」的**第一列**，旁邊有 `?` 說明。**預設關閉。**
- 打開之後，右上角回到過去那個四選一的下拉（完整封面／直式海報／文字／詳細），`A` 鍵也回到四段循環。

### Changed
- ⚠️ **表格與清單兩種呈現，預設不再顯示。** 右上角過去一直有這四個選項，現在預設只剩封面與海報。**如果你原本就停在表格或清單，升級後第一次開瀏覽頁會直接回到封面格**（沿用你上次選的卡型——上次選海報就回海報格，不會被洗成封面）。要拿回來：「設定」→「列表顯示預設」→ 打開「顯示表格與清單」。
- 為什麼要這樣：右上角改成一鍵直切之後，若畫面還停在表格、而按鈕只剩封面／海報，你一按就掉進封面格**而且回不去**——那個選項已經不在畫面上了。與其留一個回不去的狀態，不如講清楚它去哪了。

### Fixed
#### 🎞 片子多的時候，切換封面／海報終於看得到動畫
- 過去在滿頁（90 張卡）按海報，畫面會**凍住約一秒、閃一下、然後直接跳成海報**——動畫其實在播出第一幀之前就已經跑完了。實測：準備動畫要 **1199ms**，而動畫本身只有 **333ms**。
- 現在只對**畫面上看得到的那幾張卡**做動畫，看不到的直接到位（你本來就看不見它們）。準備成本 **1199ms → 93ms**，動畫是真的播得出來的。片子少於一頁時本來就正常，行為不變。
- 連點兩次會中斷前一次、不留殘影。系統設定「減少動態」時照舊直接到位、不播動畫。

#### ✨ 切換卡型不再「閃兩次」
- 上面那件事修好之後還剩一閃：舊做法是「先換版面 → 等一拍 → 才開始動畫」，中間瀏覽器有機會先把新版面畫出來，那就是第一閃。現在換版面與開始動畫落在**同一個工作單元**裡，那一閃沒了。

#### 🔋 開著瀏覽頁不動作時，畫面真的會停下來
- 側欄「瀏覽」那顆表示「你在這裡」的指示燈**永遠在轉**——而瀏覽頁正好是全站半透明模糊表面最密集的一頁，兩者湊在一起，每一幀都得整片重算。實測：燈箱開著時 **13fps**；把那顆指示燈停掉之後 **61fps**。
- 現在它**進場轉一圈就停**。你仍然看得出自己在哪一頁——那靠的是側欄的底色，不是旋轉。勾了「減少動態」的人本來看到的就是靜止的，行為不變。
- **會影響誰**：拿筆電（核顯 ＋ 省電模式）開著瀏覽頁不做任何事的時候，過去風扇會轉、電池掉得比別頁快。

### 已知限制
- **搜尋頁不在這一版的範圍內。** 搜尋結果裡每一張「本地已有」的卡各掛一顆同樣的旋轉光暈，數量可能比瀏覽頁多一個數量級。那是下一次的事。
- **手機／窄視窗仍然只有直式裁切**，拿不到「完整封面」那一種。要讓窄視窗也選得到封面格是另一件事（會動到版面契約），本版不做。
- **排序反轉在片庫超過一頁時不播洗牌動畫**，改播整頁淡入。這是**對的**——多頁時反轉排序會把整頁換成完全不同的片，「洗牌」在語意上不成立。已查證，不是 bug。
- 內部另加了一道機械檢查：新加的「永不停止的動畫」必須先進白名單，避免同樣的組合原地再炸一次。這件事你看不到。

測試數 7405 → 7408（pytest，本版新增 3 支；npm test 1293 → 1330，本版新增 37 支）。

## [0.15.1] - 2026-08-28

> 這一版只影響 **javdb 這一個來源**，但影響不小：**它換了一條取得資料的路。** 過去有些人的 javdb「永遠沒有結果」，而他用瀏覽器打得開；現在那些情況大多查得到了，而且拿到的資料更完整、封面不再有浮水印。你的資料一個位元組都沒動。

### Added
#### 🎬 javdb 換了一條路，拿得到的東西變多了
- javdb 改成**優先走 javdb 官方 App 在用的那條資料通道**，原本的網頁解析保留為備援。新的那條不通時會**自動退回**舊做法，你不需要做任何事，畫面上也不會多出任何訊息。
- 因此這兩種情況下 javdb **終於查得到**：
  - **被 javdb 擋下來的時候。** 過去只要被擋，javdb 就整個沒有結果——而「被擋」是很常見的（同一個 IP 查幾次就會遇到）。新的那條不經過那道防護。
  - **安裝路徑含中日韓文字的時候**（`C:\使用者\你的中文名字\...` 這一類）。這是舊版就有的已知限制，變通做法一直是「裝在純英文路徑」——走新的那條不會再碰到它。
- **封面不再有浮水印。** javdb 網頁上的封面正中央有一個半透明的 `javdb.com` 字樣，新的那條拿到的是**沒有浮水印的同一張圖**，尺寸完全相同（實測 5 部不同片商的片：800×538、840×566、800×438 各自逐片對照）。
- **資料變完整：片長、導演、發行商、系列、劇照，現在都抓得到了。** 這些欄位 javdb 站上本來就有，是我們過去沒抓。**備援那條也一起補齊**，所以就算哪天退回舊做法，這些欄位也還在。

### Fixed
#### 🏷 片商欄位一直寫的是發行商
- javdb 的頁面同時列出「片商」與「發行」兩欄，我們過去把**後者**寫進了片商。兩者不同的片，你的 NFO 和畫面上的片商**從來就是錯的**。
- 實例：`ABW-300` 的片商是 `PRESTIGE`，過去一直顯示成 `ABSOLUTELY WONDERFUL`（那是發行商）。
- ⚠️ **已經整理過的片不會自動更正**，要重新刮削那一部才會更新。

#### 📝 標題裡混進了「顯示原標題」和一整串日文
- javdb 的標題區塊裡有一顆「顯示原標題」按鈕，還有一段藏起來的日文原標題。我們過去把它們**全部串進了標題**。
- 現在只取真正的標題。同樣地，已經整理過的片要重新刮削才會更正。

#### 🔌 開著代理軟體時，javdb 終於連得上
- 如果你用 Clash／v2rayN 這類軟體、而且開的是「**系統代理**」模式，過去 **javdb 是全站唯一連不出去的來源**——其他來源全部正常，所以看起來就像「javdb 這個網站壞了」或「這部片 javdb 沒有」。
- 實際發生的事是：OpenAver 連 javdb 用的是一條**跟其他來源不一樣的特殊通道**（javdb 擋得比較兇，需要它），而那條通道**看不到 Windows 的系統代理設定**。它只認環境變數，而「系統代理」寫的不是環境變數。
- 現在那條通道會跟其他來源一樣讀到你的系統代理設定，也**尊重你設定裡的「不使用 Proxy」清單**。沒有設代理的人，行為跟這一版之前完全相同。
- 設定頁那個 Proxy 欄位是 **DMM 專用**的，跟這件事無關，行為沒有改變。

#### 🩺 javdb 沒有結果時，`debug.log` 裡查得出是哪一種
- 過去「這部片 javdb 真的沒有」「連不上 javdb」「被 javdb 擋下來」這三件事，**在 log 裡長得一模一樣**（而且預設還看不到）。所以每次有人回報「javdb 查不到」，我們都得來回問三輪才知道是哪一種。
- 現在這三種在 `debug.log` 裡分得出來，而且**不需要你特地開 Debug 模式**——一般啟動的黑窗就看得到。回報問題時把 `debug.log` 附上，我們一眼就知道要修哪裡。
- **畫面上刻意維持原樣**：不管是哪一種，你看到的都還是「找不到資料」。原因太技術，寫在畫面上只會讓人更困惑——這跟其他來源的處理方式一致。

### Changed
- **劇照可能換一家來源。** 過去 javdb **給不出劇照**，所以你的劇照一直來自排在後面的其他來源；現在 javdb 有劇照了，把 javdb 排在前面的人會拿到 javdb 的那一批。已經下載過的劇照**不會被覆蓋**（沿用 v0.14.7 的承諾）。**唯一的例外是「唯讀來源」的輸出夾**——那個夾子裡的劇照按「補劇照」時會被重新產生一次，因為它整個夾子的設計就是可以重產的（你的原始檔案不在那裡，一個位元組都不會動）。這不是本版的改動，v0.14.7 起就是這樣。
- **標籤會多出简体那一組。** 新的那條回的標籤是简体中文，網頁備援那條是繁體——例如同一個標籤會有「單體作品」與「单体作品」、「白天出軌」與「白天出轨」兩種寫法，瀏覽頁的標籤清單上就會各佔一格，你想篩選時要點兩次才篩得全。**我們刻意不做简繁轉換**：轉換會猜錯，而標籤是你拿來篩選的東西，猜錯的代價比多一顆 pill 大。

### 已知限制
- **素人番號仍然查不到**（`259LUXU-1234`、`300MIUM-1077` 這一類）。這跟本版無關，是各站番號寫法不同造成的（javdb 把它記成 `LUXU-1234`，吃掉了前面那串數字），要另外處理。**變通做法還是老樣子**：用進階搜尋手動指定來源與番號。
- **劇照的第一張偶爾會是封面的小縮圖**（實測 15 部裡有 2 部，147×200）。我們選擇**全部收下、一張都不丟**——丟掉它的規則會連真正的劇照一起誤刪，那個代價比較大。
- **新的那條失效時，javdb 會自動退回舊做法，你不會察覺。** 那時封面會重新帶上浮水印、遇到「被擋」也會再次沒有結果。我們自己有一顆偵測燈會轉紅，但那是給我們看的。
- **以上這些只適用「輸入番號」的搜尋。** 用關鍵字或女優名搜尋時，OpenAver 本來就只問 JavBus 與 FANZA 兩家（javdb 從來不在那條路上），所以那邊的結果不會有任何變化。
- 走**備援**那條時，「安裝路徑含中日韓文字」的限制仍然存在（變通做法：裝在純英文路徑）。
- 「開著系統代理仍然連得到 javdb」這件事**需要真的裝了代理軟體的機器才驗得出來**；自動化測試涵蓋的是「代理設定有沒有被正確讀到、有沒有真的送出去」那一段。

測試數 7204 → 7405（pytest，本版新增 201 支；npm test 1293，本版未新增前端測試）。

## [0.15.0] - 2026-08-27

> 這一版**畫面上幾乎沒有改動**——你唯一會注意到的差別是用鍵盤操作的時候。其餘全是內部整理：把同一件事的四、五份重複實作收斂成一份，讓下一次修 bug 只需要改一個地方。你的資料一個位元組都沒動。

### Fixed
#### ⌨ 用鍵盤操作時，Tab 不會再跑到彈出畫面的背後
- 搜尋頁的燈箱、以及**兩頁的劇照集**（在燈箱裡點一張劇照放大的那一層）過去完全沒有焦點鎖：按 Tab 會一路跳到蓋在底下、看不見也點不到的搜尋列與卡片上。你不知道焦點跑到哪，只能一直按到它自己繞回來——看起來就像鍵盤壞了。
- 瀏覽頁的燈箱本來是有擋的，但它的條件寫著「劇照集打開時就放開」，**所以劇照集一開，兩頁都變成整頁沒有焦點鎖**。
- 現在四個全螢幕層各自把焦點關在自己裡面：Tab 只在該層的按鈕之間循環；Esc 一次只關一層（劇照集 → 燈箱 → 頁面）；關掉之後焦點回到你原本點開它的那個地方。左右方向鍵切換候選／劇照的行為沒有變。

### Changed
> 以下四項**使用者沒有可見差異**，列出來只是因為它們動到的範圍不小。

- 全站原本有**五份各自獨立的提示訊息（toast）實作**，資料形狀已經裂成兩種、預設顏色也裂成兩種。現在收斂成單一一份。五頁的提示位置、顏色、圖示、停留時間、關閉鈕、連續兩則的覆蓋行為全部與過去逐格相同。
- 搜尋頁的標題／中文標題／演員三組行內編輯原本是三份逐字重複的程式碼，其中防止「編輯到一半候選被換走、按確認寫到別部片上」的那道守衛**被前後修過四次（每一份各修一次）**。現在四個欄位共用同一份，下一次修正只需要改一個地方。
- 設定頁與掃描頁的 8 個「?」說明浮層原本是複製貼上的樣板，現在收斂成一支共用元件。
- 新增一道機械檢查：前端狀態若有兩塊程式碼不小心用了同一個名字（後者會**無聲**蓋掉前者，沒有任何錯誤訊息），`npm run lint` 會直接擋下來。順手刪掉一段已經被蓋住、改了也不會有作用的死碼。

### 已知限制
- 焦點鎖只涵蓋**四個全螢幕層**（兩頁的燈箱與劇照集）。重新刮削、資料夾選擇、覆蓋警告那幾個彈窗沿用既有行為（它們有自己的 Esc 關閉），這一版沒有動它們。

測試數 7202（pytest，本版未新增 Python 測試；npm test 1240 → 1288）。

## [0.14.10] - 2026-08-25

> 這一版修兩件跟**網路不穩**有關的事：**開不起來**（開著代理軟體時啟動崩潰），以及**搜尋結果會莫名其妙變少**（接的 metatube 來源抖一下就永遠消失）。沒有新功能，你的資料一個位元組都沒動。

### Fixed
#### 🔌 開著代理軟體（Clash／v2rayN／公司代理）時，App 不再一啟動就崩潰
- 有使用者回報按下 `OpenAver_Debug.bat` 之後，畫面直接吐一段 Python 錯誤訊息就結束，什麼都沒開起來。
- 原因：App 啟動時會先「問自己一句『好了沒？』」來確認伺服器就緒。這句問話**被送去了你的代理軟體**——即使問的對象是這台電腦自己（`127.0.0.1`）。代理轉不到還在啟動中的伺服器，直接把連線掐掉，App 就當場崩了。
- 現在這句問話**無條件不走代理**。連自己本來就不該經過代理，瀏覽器（WebView2）本來就是這樣做的，只有這支 Python 請求漏掉了。

#### 🩺 啟動失敗時，畫面上的話變得有用了
- 過去不管什麼原因失敗，都只有同一句：「伺服器啟動逾時。請檢查是否有其他程式佔用**端口 8000**。」——而 OpenAver 實際用的根本不是 8000（它會自動挑一個 49152 起跳的空號）。**照著那句話去查，永遠查不到東西。**
- 現在分成兩種說法，因為那是兩件不同的事、下一步也不同：
  - **等太久**（真的逾時）→ 告訴你**實際用的那個端口號**。
  - **伺服器程序自己掛了** → 不會再讓你乾等 30 秒，**幾秒內就告訴你**，而且不會把你導去查端口（那跟端口無關）。
- 兩句話都會直接印出 `debug.log` 的**完整路徑**，Windows 上是 `C:\Users\你\OpenAver\logs\debug.log`、Mac 上是 `/Users/你/OpenAver/logs/debug.log`——貼進檔案總管／Finder 就能打開。**過去只寫「請查看 debug.log」，沒說在哪裡。**

#### 📓 log 裡真的查得到原因了
- 過去啟動失敗時，`debug.log` 有時候是空的——訊息叫你去看它，打開來卻沒有任何線索。
- 現在失敗時**一定會留下最後一次連線失敗的真正原因**（例如被防毒／防火牆擋掉）。只留一次，不會洗版。
- 伺服器背景程序如果因為**程式碼層級的例外**掛掉，完整的錯誤內容也會寫進 `debug.log`（但**不會**丟到畫面上——那對一般使用者沒有意義）。少數情況查不到真因（例如 OpenAver 選好的那個端口在啟動前一刻被別的程式搶走），畫面仍會告訴你程序中止了，但 `debug.log` 只會有探活失敗的紀錄。

#### 🔁 metatube 來源抖一下就消失的問題修好了
- 你接的某個 metatube provider 只要**逾時一次**（網路抖一下、對方忙一下都算），它就會被踢出自動搜尋，而且**再也不會回來**——除非你自己想到要跑去設定頁按「重新測試」。畫面上沒有任何提示，你只會慢慢覺得「怎麼有些片刮到的資料變少了」。
- 現在改成**冷卻幾分鐘後自動再試一次**：好了就恢復正常，還是壞的就再冷卻一次。**不需要你做任何事。**
- 反過來也有照顧到：如果那家是**真的長期掛掉**（不是抖一下），你**不會**每打一個番號就被它拖著等一次逾時——每個冷卻窗口最多只付一次代價。
- 給 AI 助理用的「目前實際會搜哪些來源」清單，現在跟真正搜尋時會用到的來源是同一份判斷，不會兜不起來。
- 起點是外部貢獻者 [@sheepzacks](https://github.com/sheepzacks) 的 [PR #155](https://github.com/slive777/OpenAver/pull/155)：他把根因找出來了（本地的失敗紀錄沒有到期時間），我們採用他的診斷、換成冷卻式的修法。

### 已知限制
- **設定頁的來源膠囊會比實際行為慢半拍。** 冷卻結束、後端已經在下一次搜尋悄悄重試的那段時間裡，膠囊仍然顯示灰色（不可用）——因為它報的是「最後一次**真正驗證過**的結果」，不是「我猜它現在應該可以了」。這是刻意的：寧可畫面慢半拍，也不要它先講一個還沒被證實的好消息。
- 承上：**要它轉回正常，需要那個來源真的回一次結果**。如果它每次都回「這裡沒有這部片」（番號不在該來源，日常最常見的結果），那不算一次成功的驗證，膠囊會一直維持灰色，而後端每次搜尋仍會多問它一次（一趟很快的往返）。
- 這一版修的是「連自己」被代理攔截的情況。如果你的啟動失敗**另有原因**（例如某些安全軟體在更底層攔截），App 仍然開不起來——但至少會給你一句看得懂的話和一個查得到東西的 `debug.log`，而不是一段 Python 錯誤訊息。
- 「開著代理仍能正常啟動」在 Windows 真機上驗證過（環境變數與登錄檔兩種代理設定都試過）；自動化測試涵蓋的是環境變數那一種。

測試數 7130 → 7202（pytest；npm test 未變動）。

## [0.14.9] - 2026-08-25

> 這一版整理的是「同一顆按鈕、同一條搜尋列，在不同分頁與不同頁面上行為不一樣」的那些地方。功能沒有新增，你的資料一個位元組都沒動。

### Fixed
#### 🔀 清除鈕（✕）只管你正在看的那一頁
- 過去在**女優牆**按 ✕，會把**影片牆**的搜尋字與條件標籤一起清掉（反過來也一樣）——你回到影片牆才發現條件不見了，而且不知道是什麼時候沒的。
- 那顆 ✕ 連「該不該出現」都是共用的：影片牆掛著條件時，切到女優牆即使一片空白也會冒出一顆 ✕，按下去清掉的是另一頁的東西。
- 現在**顯示條件與清除範圍都只看當前分頁**。

#### 💾 女優分頁打的字，重新整理後還在
- 女優牆搜尋框打的字，過去重整就沒了——而影片牆那半一直都會記住。兩邊行為不一致這件事沒有理由，現在補齊。

#### 🖼 最愛女優的大卡，切頁回來不再消失
- 搜尋字精準命中某位最愛女優時，影片牆上方會出現一張她的大卡。過去切到別頁再切回來，那張卡就不見了，要把名字重打一次才回得來。

### Changed
#### 📐 搜尋頁與瀏覽頁的搜尋列，切換時看起來不會動
- 兩頁搜尋列的外框與輸入框，在 **1024／1100／1280／1440／1920 五個桌機寬度下位置與尺寸完全相同**。過去在 1100px 下，輸入框一邊是 680px、另一邊是 430px，切頁時會看到它變形。
- 搜尋列下方那行說明文字搬進了空狀態。順帶的好處是——那行字在手機上本來是隱藏的，**搬過去之後手機反而看得到了**。
- 為了讓兩頁真的對齊，**搜尋頁右側會永遠保留一條約 15px 的空白帶**。那是必須付的：瀏覽頁的同一段空間真的被捲軸佔著。

#### 📱 手機上掛兩枚條件，搜尋列不再撐成三行
- 過去在手機上掛兩枚條件標籤，它們會**各佔一行**，加上第三行的輸入框——而那行因為有條件時提示字會被清空，看起來就是一整條空白。
- 搜尋列左右兩側預留的空間原本是照「按鈕全部出現」的最壞情況寫死的，但那些按鈕多半只出現兩顆。現在壓到實際需要的大小，**可用寬度多出 42%**，常見的兩枚標籤回到同一行、搜尋列回到單行高度。

#### 🎯 拖放提示層，兩頁終於長一樣
- 把檔案拖進**搜尋頁**與**掃描頁**時蓋上的那層提示，過去進場動畫（一邊會放大、一邊只有淡入）、提示字級、內距、圖示陰影全都不同。現在以搜尋頁那一版為準統一。

### 已知限制
- **那條捲軸空白帶需要 Chrome 94／Firefox 97／Safari 18.2（iOS 26）以上**。更舊的裝置會安靜地維持今天的樣子——不會破版，只是沒修到。
- **手機上也會保留那條空白帶**。手機本來就沒有「兩頁對不齊」的問題，這是為了整站行為一致付的代價。
- 手機上兩枚**比較長**的條件標籤（例如兩個女優名字）仍然會各佔一行，輸入框也仍然在第二行——390px 的橫向空間裝不下。
- **拖曳檔案到一半按 Esc 取消時，提示層可能會卡住擋住整頁**，需要重新整理頁面。這是既有行為，本版沒有改動它（新舊做法在這個情況下表現相同）。

測試數 7130（pytest，本版未新增測試；npm test 1223 → 1240）。

## [0.14.8] - 2026-08-24

> 這一版只影響**用瀏覽器**開 OpenAver 的人（區網手機、Linux／NAS 上的伺服器模式）。桌面版（Windows／Mac 應用程式）的操作行為一個位元組都沒動。

### Added
#### 📂 瀏覽器裡終於選得到資料夾
- 過去在瀏覽器按「加檔案」「加資料夾」（搜尋頁）、「輸出資料夾」（設定頁）、「加來源」（掃描頁），只會跳一句「此功能需要在桌面應用程式中使用」就結束——**四個入口全是死路**。現在會開一個資料夾選擇視窗，直接瀏覽這台電腦上真實存在的資料夾：麵包屑、單擊資料夾就進去、上一層、畫面上一顆常駐的「選取此資料夾」（對齊 Google Drive／Dropbox 網頁版的習慣，不用學新東西）。
- **它記得你上次選過的地方**，而且三個入口各自記自己的——掃描頁選過片庫，不會害設定頁的輸出夾也跳到片庫那層。
- 第一次打開時的起點是**你第一個掃描來源的上一層**（多半就在你接下來想選的地方附近），不是 OpenAver 的安裝目錄。完全沒設定過來源才從檔案系統根目錄開始。
- 搜尋頁選定資料夾後，匯入的是**那一層裡的影片檔**（跟桌面版一樣的展開行為），不是資料夾路徑本身。

#### 🐧 Linux 伺服器部署文件
- 新增 [`docs/linux-server.md`](docs/linux-server.md)：把 OpenAver 架在家裡那台沒有螢幕的 Linux 機器（舊主機、NAS、Proxmox LXC、樹莓派）上，用區網其他裝置的瀏覽器操作。從下載正式版原始碼、建 venv、開啟伺服器模式、查出區網網址、啟動，到升級與**忘記密碼時的本機重設指令**，每一條指令都在乾淨環境實跑過。另附開機自動啟動（systemd）的選配設定。
- 文件裡明講了三件容易踩空的事：**沒有「一鍵更新」按鈕**（那顆鈕是桌面版限定，從瀏覽器按會得到 403）、**重設密碼之後必須重啟伺服器**（執行中的伺服器把設定快取在記憶體，不重啟看起來就像指令沒生效）、以及伺服器模式的門禁**只分「本機」與「其他所有人」**——它不會去檢查對方是不是真的在你家網段內，所以**不要**把 port 轉發到網際網路、**不要**架在有公網 IP 的雲端主機上。
- 同一份文件附了一段 **Docker（實驗性）**：Dockerfile 與 compose 範例在 x86_64 Linux 上實際建置並執行過，但**未在 Synology／QNAP／樹莓派這類 NAS 環境驗證**，僅供參考。官方 Docker image 仍在規劃中。
- README 加了一行連結指過去，其餘內容未動。

### Fixed
- **把資料夾拖進掃描頁，不再毫無反應。** 過去覆蓋層關掉之後什麼都不會發生——沒有提示、沒有錯誤，像當機一樣。現在會說明「瀏覽器無法直接讀取拖放檔案的路徑，已為您開啟資料夾選擇器」並直接把視窗開起來。（瀏覽器基於安全限制不會把拖入檔案的絕對路徑交給網頁，這件事本身修不了；能修的是「靜默」。）
- **影片播不出來時，話說清楚了。** 過去一律顯示「這部片現在放不出來——可能是檔案位置拿不到，或者這個格式瀏覽器不支援」。現在會分兩種：瀏覽器不支援這個格式／檔案本身壞了 → 建議你改用電腦上的播放器（VLC／PotPlayer）；其他情況（中斷、網路、路徑失效）→ 沿用原本那句。
- **按了「取消」就真的取消。** 選定資料夾後如果回應比較慢，這段期間按 Escape／X／取消（甚至重開一個新的選擇器），舊的那次選取不會再回頭作用在畫面上——不會蓋掉你剛選的新資料夾，也不會把新開的視窗關掉。

### 已知限制
- **瀏覽器下「加檔案」與「加資料夾」現在是同一件事**（都是整夾匯入第一層的影片檔）。桌面版可以跨資料夾任意挑單一檔案，瀏覽器沒有這個能力——伺服器端只能「列出這一層的影片檔」。要挑單片請用桌面版，或先把那幾片放進同一個資料夾。
- **這個視窗看得到片庫以外的資料夾。** 它不限制範圍，能看到伺服器讀得到的任何目錄**名稱**（只有名稱，不會外流檔案內容）。區網存取本來就要先過伺服器模式與密碼那兩道關；這是刻意的取捨——限制範圍會讓「選一個還沒設定過的新資料夾」這件事直接做不到。
- **掃描頁「加來源」不再自動幫你展開手動輸入框**（那個入口還在，只是不再自動打開）。
- 選定的資料夾如果片很多，視窗會先關掉、檔案列表稍後才出現，中間沒有進度提示（桌面版本來也是這樣）。
- 新增的介面文字目前只有繁體中文，其他三種語言會顯示中文（下個 milestone 統一補）。
- 影片錯誤分級依賴瀏覽器回報的錯誤代碼，少數瀏覽器／版本回報不準確時可能分到「其他情況」那一類。
- Linux 部署文件的 Docker 段**沒有在 NAS 上驗證過**；`docs/linux-server.md` 也只有繁體中文版。

測試數 7105 → 7130（pytest；npm test 1189 → 1223）。

## 0.14.x 系列（早期，v0.14.0 ~ v0.14.7, 2026-08-19 ~ 2026-08-24）

- **拿不到的時候畫面上要有話說 ＋ Windows 啟動與安裝修復**（v0.14.0）：燈箱原圖載不到、瀏覽器播放失敗改為就地顯示提示（過去是一片黑／看起來一切正常）；一般身分安裝的 WebView2 認得出來、提示視窗改用系統訊息視窗、log 不再把「視窗沒顯示」記成「用戶取消」、網址自動進剪貼簿、安裝視窗不再閃退；罩杯排序／篩選支援 L 以上。
- **檔名版本後綴自動變成標籤 ＋ 封面屬性標籤**（v0.14.1）：檔名帶 `-C`／`-U`／`-UC`／`-leak`／`-4K`／`-VR` 的片會被認出來寫成「中文字幕」「無碼破解」「無碼流出」「4K」「VR」標籤，整理／掃描／補齊／唯讀來源四條寫入路徑一致；瀏覽頁標籤篩選同時比對手貼的自訂標籤；封面左上角可顯示屬性標籤（預設關閉的 opt-in、最多 3 顆、可逐項關且不動標籤本身）。
- **分集片合併呈現 ＋ 批次整理撞名標記**（v0.14.2）：同一資料夾裡 `-cd1`／`-cd2` 的分集片在瀏覽頁合成一張卡、封面標出段別、瀏覽器播放自動接續下一段、「從收藏移除」整組連動（磁碟檔案一個都不刪）；批次整理撞名改為列表上的常駐警示標記（點開看撞到誰），不再只給一個 4 秒 toast。另修「同片兩種容器被誤認成兩段」與「整組刪除後其餘各段縮圖快取沒清乾淨」。
- **一鍵精選 ＋ 冷門女優也能收藏**（v0.14.3）：燈箱標題前多一顆星，點一下就是精選；番號前的那顆星在四種看片方式裡都跟著，漏斗選單新增「只看精選」可與其他條件疊加且會被記住，分集片一次點兩段共用，也開放給 AI 助理批次呼叫。另：查不到資料的冷門／早年女優，只要片庫裡已經有她的片就能收藏——用片封面右側自動裁一張當頭像，之後可換圖／上傳／調對焦。
- **發售日可點成條件 ＋ 女優牆眼睛鈕**（v0.14.4）：發售日跟片商、系列一樣可點出條件，並可切成等於／小於等於／大於等於或自訂年月區間；女優牆補上與影片牆同一顆眼睛，整面牆一起攤開身高／罩杯／三圍且數字可點成條件；燈箱換片箭頭改對齊封面並卸掉兩顆玻璃圓盤（修好手機上想點星星加精選、畫面卻跳到上一片）；窄螢幕女優卡名字被數字擠掉的問題修復；封面對焦拖曳中不再被誤判成換片。
- **第三方套件與來源紀錄補齊**（v0.14.5）：對使用者零可見改動；`web/static/vendor/` 底下每個檔案都查得到版本與授權，第三方來源集中記在 `docs/THIRD_PARTY.md`，並加守衛擋「換了套件卻忘了更新紀錄」。
- **metatube 來源的圖片路徑修復**（v0.14.6）：劇照不再因為自家網域白名單漏收冷門圖床而整排 403 破圖（改走你自己那台 metatube 取回，封面在 v0.13.6 已修）；抓不到原始圖床時整理／補齊／唯讀來源三條路都會自動改走 metatube，**存檔一律優先原始網址**（代理那條會重壓 +3%）；metatube 掛掉時封面仍出得來。起點是外部貢獻者 [@sheepzacks](https://github.com/sheepzacks) 的 [PR #151](https://github.com/slive777/OpenAver/pull/151)。
- **補劇照不再覆蓋你自己放的圖**（v0.14.7）：`extrafanart/` 資料夾裡已經存在的圖一律跳過、只補缺的那幾張，不管是手動放的、Jellyfin 留下的還是上次抓好的；單片／批次／補劇照三個入口一體適用。

## 0.13.x 系列 (v0.13.0 ~ v0.13.14, 2026-08-01 ~ 2026-08-15)

- **封面正典位置與媒體伺服器整合收斂**（v0.13.0、v0.13.2～v0.13.4，feature/109 起）：唯讀來源封面產出邏輯先收斂成單一入口（v0.13.0，純內部、零行為變更）；修好「補齊 Jellyfin 圖片」把只有 `-poster` 的片封面原圖就地裁小且不可還原、以及封面被記成 `-fanart` 誤判成缺 NFO 兩個問題（v0.13.3）；接著把封面「正典位置」翻面——不再多產一張同名封面、`-fanart` 升格為正典，修好接 Jellyfin／Emby／Kodi 時封面牆橫式圖塞進直式格子（v0.13.4）；外部媒體管理器設「關閉」時唯讀來源不再多產兩張沒人看的圖、省空間，同時修好 HEYZO 站方改版造成的搜尋整個查無問題（v0.13.2）。
- **第三方 NFO 誤判缺漏遭覆寫修復**（v0.13.5）：片庫裡若有 Jellyfin／Emby／其他刮削器留下的 `.nfo`，裡面明明有的演員／片商／發行日／分類標籤過去會被誤判缺漏、上網刮一份回來覆寫掉——四種讀不到的寫法全部修好；另修「欄位有標籤沒內容當成已有資料」「全空白標籤吃掉後面合法值」「補完寫完檔案卻整片報失敗」「同一女優兩個下載互相覆蓋」等靜默 bug。
- **區網與帳密安全性全面強化**（v0.13.1、v0.13.6～v0.13.8）：升級元件消除 17 個已知 CVE、整理與唯讀庫產出加上路徑逃逸最終防線（v0.13.1，feature/110）；metatube 帳密三處外流出口封閉、圖片來源改宣告式白名單（v0.13.6）；區網連線可設 4 位密碼，擋下的人看到偽裝頁而非登入畫面，連錯自動鎖定並加公網暴露警告（v0.13.7）；密碼保護開啟後可給 AI agent 專屬連線 token（改密碼即自動撤換），設定頁的 API 金鑰／metatube token 不再完整送進瀏覽器、只回後四碼（v0.13.8）。
- **瀏覽篩選與女優牆體驗升級**（v0.13.9～v0.13.11）：瀏覽頁篩選從「點一下就整段蓋掉關鍵字」改成可疊加的條件標籤（女優／標籤／片商／導演／系列／廠牌可同時掛上、取交集，v0.13.9）；女優燈箱的年齡／身高／罩杯也能點出條件，並可改成以下／以上／自訂區間（v0.13.10）；女優牆新增「從片庫加入女優」面板，依片數由多到少排序、一鍵收藏庫內尚未加入最愛的人（v0.13.11）。
- **FC2 來源修復**（v0.13.12～v0.13.13）：FC2 來源鏡像站整站被 Cloudflare 擋到搜尋 100% 查無資料，改打官方站恢復搜尋，並讓劇照支援的檔案類型對齊 Jellyfin、修好部分 Windows 上側邊欄 logo 顯示成破圖（v0.13.12）；官方站已下架的片再補一條備援來源 FC2-javten（僅限 Windows 桌面版、重刮彈窗手動選用，v0.13.13）。
- **桌面影片牆新增直式海報卡型**（v0.13.14）：桌面工具列的模式選單多一條「直式海報」，與「完整封面」並列可切換、切換即時記住；只是同一張封面在畫面上的裁切顯示，不動封面原檔、不碰 NFO。

測試數 5640 → 6728（pytest；npm test 287 → 825；v0.13.13 全文未附測試段落，範圍取有紀錄的相鄰版本）。

## 0.12.x 系列 (v0.12.0 ~ v0.12.10, 2026-07-14 ~ 2026-07-24)

- **焦點裁切上線與收尾**（v0.12.0～v0.12.1、v0.12.3，feature/98/99/101）：無碼封面（FC2／素人／uncensored）刮削或掃描時自動偵測人臉、以臉為中心裁切封面與燈箱大圖，有碼片維持原樣零成本；燈箱補上「自動對焦→左右拖曳微調→存檔」正式互動、唯讀來源也納入自動對焦、牆上小格裁切改與燈箱大圖同精準度；後續再擴及 Jellyfin/Emby/Kodi 媒體伺服器海報，對焦等待動畫改視覺連續交棒，影片對焦鈕改為只在窄螢幕／手機才出現。
- **女優照片管理更完整**（v0.12.2、v0.12.4，feature/100/102）：女優頭像可自行上傳照片、也能像影片一樣手動對焦裁切；修好改過名（有別名）的女優在照片挑選視窗選本機候選必失敗、雲端重抓永遠查不到圖的問題，重抓改為自動輪流用她的每個名字查詢；順手讓搜尋詳情、燈箱、劇照瀏覽都能用滑鼠滾輪切換上一片／下一片。
- **技術債清償**（v0.12.5，feature/103）：約 130 行硬編碼中文文字收進語言檔；拖曳檔案與貼上檔名的番號辨識規則統一為同一套後端規則（不再兩套邏輯漂移）、辨識失敗改明確提示；清除死碼、logger 與中文硬編碼機械 lint 守衛正式上線。
- **唯讀來源（雲端／NAS 分享盤）功能大幅強化**（v0.12.6、v0.12.7，feature/104/105）：已經整理好的唯讀庫（旁邊有 `.nfo`／封面）改為直接就地讀用、不再無腦重刮，唯讀片的放大鏡補料／齒輪重刮／補劇照三顆鈕全面解禁（產物仍一律落獨立輸出夾、來源零寫入）；並把散在四處、常常改一處漏改一處的「記帳與回報」邏輯收斂成一份共用實作，順手修好唯讀破圖、原文標題被清空等一批連帶 bug。
- **整理前手動修正 + 桌面體驗收尾**（v0.12.8～v0.12.10，feature/106/107/108）：拖檔進搜尋頁後可在整理前直接改演員名單、幫無碼片補發售日；桌面版加上開機自動檢查更新（可關）、修好深色主題幾顆看不清的按鈕；手機瀏覽封面吃滿寬度、觸控裝置封面不再擠一排操作圖示（改進燈箱操作）、女優牆卡片大小對齊影片牆不再忽大忽小。

測試數 5233 → 5640（0.12.0 全文未附測試段落，範圍取最早可查的 0.12.1 起；前一系列 0.11.12 收尾值為 5030）。

## 0.11.x 系列 (v0.11.0 ~ v0.11.12, 2026-06-27 ~ 2026-07-12)

- **v0.11.0**：JavBus 過度泛用清償 + exact 番號搜尋改優先序 cascade（feature/85）——直接搜番號改為依你拖曳的來源優先順序逐一查詢（cascade，命中即回），JavBus 不再無視優先序搶先短路；DMM proxy 透傳修復、前綴搜尋 `type` 參數修正；拔除已死的 JavBus variant（同番號多版本）探查死碼。
- **v0.11.1**：JavLibrary 同番號多版本手動切換（feature/86）——搜尋框直搜／燈箱換來源／結果卡替換來源三入口皆可看封面手動挑撞號版本（游標預設停最新發行日），桌面 standalone 限定（需 CF transport）。
- **v0.11.2**：`core/database.py` 模組化拆分（feature/87）——2,152 行單檔拆成 `core/database/` 套件（六個領域子模組 + 永久 re-export facade），消除 `AliasRepository`／`TagAliasRepository` 鏡像重複碼（共用泛型基類），零行為／API／schema 變更。
- **v0.11.3**：唯讀來源生成本地媒體庫「off 風味」首發（feature/88）——scanner 來源可勾「唯讀」＋設輸出夾，來源零寫入下生成每片一資料夾的本地庫（NFO + 封面 + 劇照）並直接寫進 DB，供 OpenAver 自身瀏覽／串流播放雲端原檔；給 Emby/Jellyfin/Kodi 的 media-server（`.strm`）風味延後至下一版（feature/89）。

- **v0.11.4**：唯讀產生庫「地基」+ 掃描頁「試過」記憶 + 來源刪檔清死卡（feature/89）——生成片記住輸出夾（`videos.output_dir`）重刮原地更新不長重複夾、off 風味固定輸出夾免設定；試過／已生成的片不再被「缺資料」嘮叨、刮不到只試一次；唯讀網盤掉線明確警告不誤報成功；來源刪檔後 DB-row-only 清死卡（零檔案刪除）。
- **v0.11.5**：唯讀來源生成媒體伺服器庫「.strm 風味」+ 跨機器路徑映射 + 唯讀寫入全面封鎖（feature/90）——唯讀來源可產出 `.strm` 捷徑檔給 Emby/Jellyfin/Kodi 掃描播放，跨機器「播放端路徑替換」規則讓 `.strm` 內路徑翻成播放端看得懂的形式、改規則一鍵同步改寫既有 `.strm`；同時把唯讀來源「零寫入」補到滴水不漏（勾唯讀破壞性確認、四個寫入入口全面停用、切模式清舊媒體卡、產生中斷乾淨收尾）。
- **v0.11.6**：跨機器路徑映射（WSL2+UNC）讀寫全棧收斂 + DB-key 命名空間守衛（feature/91）——純 correctness 重構：修好「讀取端忘了在碰磁碟前把映射路徑反解回本機路徑」導致縮圖／封面／串流跨機器讀不到的一類 silent bug，以及「已反解路徑又被裸餵回 DB key」導致重刮掉使用者標籤、女優照 403 的另一類；兩支 AST 結構守衛擋死回歸。

- **v0.11.7**：搜尋頁體驗優化（feature/92）——搜尋列右側控制項不再擠壓變形、整理入口常駐可見更好找、整理入庫「飛入側邊欄」動畫加強為三段節奏，並抽成可跨頁複用的共用元件；順手修好「重新整理已有結果頁面時出現兩組 ✕」的 latent bug。
- **v0.11.8**：各刮削來源評分/簡介補進 NFO（feature/93，NFO-only）——七源評分、六源簡介寫進 `<rating>`/`<plot>`，純服務媒體伺服器（Jellyfin/Emby/Kodi）用戶，OpenAver 自身介面不顯示；順手美化燈箱中繼資訊視覺層次。
- **v0.11.9**：掃描頁「補資料」升級成逐片進度卡 + 命中封面飛入圖書館（feature/94）——補資料時逐片可視進度（搜尋中／命中／查無／失敗／唯讀跳過），真封面命中飛入側邊欄「瀏覽」入口。
- **v0.11.10**：設定頁命名區膠囊化 + 列表生成兩層 IA 重排（feature/95）——檔案命名格式從手打字串改成「變數膠囊 + 字面文字」視覺化編輯器、資料夾層級改動態清單（硬上限 3 層）；列表生成設定拆成日常常用（常駐）+ 離線 HTML 匯出（摺疊進階）。
- **v0.11.11**：前端靜態守衛 pytest → lint 全面遷移（feature/96，test-deflation）——純內部工程里程碑（零產品碼、對使用者隱形）：把 `test_frontend_lint.py` 裡「讀原始碼做字串/結構斷言」的守衛搬回工具層（新增 `i18n_lint`／`static_guard_lint`／`css-guard` 三支 `.mjs` + eslint `SEL_*` 家族），`test_frontend_lint.py` 16,749→5,041 行（−70%）、36 個真 contract class relocate 進 `frontend_contracts/`；north-star＝「能用 lint 機械處理的不進 pytest、不耗 Codex 審」。
- **v0.11.12**：javdb 在 released 版復活（feature/97）——打包瘦身刪除所有 `.dist-info` 導致 curl_cffi import-time 拋 `PackageNotFoundError`、被靜默吞掉，released Windows/macOS 版 javdb 從第一天就零 HTTP 請求；不再剝除 dist-info 即修復，並補打包產物 runtime 驗證守衛（`verify_artifact_imports.py` + dist-info 靜態檢查 + CI verify job）堵死 dev-only 測試盲區；本版另 bundle 兩個後續修復：非 ASCII 安裝路徑 `curl error 77`（CAINFO 改用 ANSI code page 編碼）+ 番號 7 字母前綴拖檔截斷修復。

測試數 4735 → 5523（v0.11.10）→ 4985（v0.11.11 test-deflation：守衛遷 lint 層等價承接，−538 非覆蓋損失）→ 5030（v0.11.12，+45：打包產物驗證守衛 + javdb CAINFO + 番號 cap 對齊回歸鎖）。

## 0.10.x 系列 (v0.10.0 ~ v0.10.11, 2026-06-18 ~ 2026-06-24)

- **來源穩定性 + 測試硬化**（v0.10.0，feature/73）：8 源真實番號健康金絲雀 smoke（三態 quorum 判讀）+ avsox 復活（站方轉 SPA 後改打背後 JSON API）+ Tokyo Hot 單字母無碼番號查無修復 + 覆蓋率地板 84%（`cov-floor`）+ 五項高風險模組（含會改寫用戶 NFO 的 `nfo_updater`）離線單元測試債清償。
- **前端呈現與發現性優化**（v0.10.1～v0.10.2，feature/74/75）：進階搜尋畢業為永久常駐核心，隱形長壓手勢全面移除、改「來源膠囊」處處可見觸發挑源／換源；搜尋詳情資訊密度重排、封面正面裁切共用規則、行動裝置基礎相容（scroll trap／星空門檻／觸控 overlay）補齊。
- **開發工具鏈硬化 + 前端離線可靠性**（v0.10.5～v0.10.6，feature/78/79）：`ruff` + eslint/stylelint 進 CI 擋 PR（先前純本地、沒人本地跑就漏）；GSAP/Alpine/圖示字型改本機載入斷網仍可用、前端錯誤自動記錄；`requirements.txt` 精確鎖版並把 Starlette 鎖至最新修補版清除已知 CVE。
- **MPA 跨頁轉場 + Fluent 材質統一**（v0.10.3～v0.10.4，feature/76/77）：純 CSS View Transitions 讓 sidebar 切頁白屏閃爍改平滑淡換（Showcase 因常駐動畫維持硬切）；全站材質收斂成單一 token 系統的 6 角色（Mica canvas／Glass shell／panel／caption／overlay／Media frame）+ 浮動圓角玻璃 chrome 擴及 search／settings／scanner。
- **LAN 伺服器模式 + 手機體驗完整化**（v0.10.7～v0.10.11，feature/80/81/82/83/84）：一鍵把桌面 App 開放給同區網手機／平板瀏覽（dual-listener + 區網存取閘門）；手機加主畫面圖示、封面／燈箱左右滑換片、窄螢幕破版修補；Windows 系統匣關閉行為（最小化背景執行）；燈箱封面比例自適應不留白 + 行動星形爆射相似探索面板；Windows 雙擊安裝捷徑 + Help 頁一鍵更新按鈕。

測試數 4089 → 4735。

---

## 0.9.x 系列 (v0.9.0 ~ v0.9.11, 2026-05-29 ~ 2026-06-13)

- Scraper Federation + metatube HTTP 聯邦（30 provider Parts Bin + promote/demote + 無碼 staged + SSRF 驗證）、Settings 分區 IA + 資料驅動來源 schema + 進階重刮彈窗
- Active Row 拖曳順序成搜尋路由唯一真理（拔除 primary_source）；async-offload 慢 I/O 移出 event loop（NAS stat / sqlite / config / 同步 HTTP）+ config 鎖/原子寫 + AST 守衛；封面三態（skeleton/shimmer/破圖）+ Showcase console 清零
- 新增來源：JavLibrary（BETA，桌面借 PyWebView 過 Cloudflare）+ avsox 復活（轉 JSON API）+ Tokyo Hot 單字母番號修復；8 源真實番號健康金絲雀 smoke（三態 quorum 判讀）+ cov-floor 84% 流程地板
- 本地 WebP 縮圖快取（opt-in，SSD 出圖不碰 NAS + blur-up 燈箱）+ 燈箱單筆刪除（只刪 DB row）；VR 投影標籤保留 + 自動 VR tag
- 外部媒體管理器相容（Jellyfin/Emby/Kodi 四態：poster/fanart 命名 + cd1/cd2 合併 + NFO 補欄 + Scanner 識別外部封面）；dim 暗色主題色彩編碼修復；進階搜尋畢業為永久常駐核心

測試數 2937 → 4089。

## 0.8.x 系列 (v0.8.0 ~ v0.8.10, 2026-04-28 ~ 2026-05-28)

- Charter Pilot：Fluent 2 視覺語言全站統一（§1–§6 + ease/DURATION 三角色）+ Ghost-fly Lightbox 共用化 + Alpine 釘版四插件 + 全站通知中心
- 全站前端 ESM 模組化（Import Maps，巨型單檔全解體）+ lint toolchain（eslint flat config + stylelint，frontend_lint 905→450）
- 以圖搜圖 CLIP Beta 出貨後轉向純規則式相似度排序器（拔 ML 依賴、主 ZIP 271MB→43MB）
- Tag Alias 跨語言系統 + Search→Showcase pipeline 即時化（GhostFly + DB 即時 upsert）+ Onboarding Scanner-first 翻轉 + SSRF 白名單 + 女優查詢 json_each 重寫

測試數 2705 起（系列歷經 CLIP 上下架與 lint 瘦身，末值未單列）。

## 0.7.x 系列 (v0.7.0 ~ v0.7.8, 2026-04-10 ~ 2026-04-26)

- Agentic AI API 平台首發（batch-search / generate-from-ids / enrich-single / collection-sql / capabilities manifest）
- User Tags 三層整合（DB + NFO `<user_tag>` + API + Search/Showcase 雙頁 UI）
- Actress Favorite + Showcase 女優模式（actresses DB + Orchestrator 4 路並行 + 女優 Grid/Lightbox/Hero + GSAP）+ Actress Alias CRUD
- Scanner 一鍵補完（missing-check + batch-enrich SSE 分批）+ Ghost Fly 轉場 + WinFsp/rclone 掛載相容 + 劇照幽靈 URL 修正

測試數 1818 → 2705。

## 0.6.x 系列 (v0.6.0 ~ v0.6.7, 2026-03-29 ~ 2026-04-09)

- 四語系 i18n 建立（繁中/簡中/日文/英文，~477 key + `core/i18n.py` + `window.t()` + JavBus lang 連動）
- Agentic AI API 初版（batch-search / generate-from-ids / enrich-single / collection-sql / capabilities）+ OpenAI Compatible 翻譯 Provider
- Alpine.js 技術債清理（6 頁統一 `Alpine.data()` 註冊、移除 bridge.js/init.js、SearchCore 全域消除）
- Scanner Jellyfin 圖片手動觸發 + TTL 快取 + 前端動效補強（checkmark/shake + Load More stagger + alert→toast）

測試數 1366 → 1634。

## 0.5.x 系列 (v0.5.0 ~ v0.5.5, 2026-03-25 ~ 2026-03-28)

JavBus Scraper 完全重寫（移除 jvav 第三方依賴）+ Video Model 擴充（director/duration/label/series/sample_images）+ Sample Gallery 元件（extrafanart 全鏈路）+ Metadata Pipeline 補齊（NFO 讀寫全欄位）+ Maker 名稱對照表重建（雙層 JSON + shared loader）+ DMM 模糊搜尋 + 全來源欄位補齊（Jav321/AVSOX/HEYZO/FC2/D2Pass）+ 字幕檔自動偵測搬移 + Proxy direct 模式。測試數 1007 → 1366。

## 0.4.x 系列 (v0.4.0 ~ v0.4.4, 2026-03-08 ~ 2026-03-19)

GSAP 搜尋頁動畫系統（SSE 漸進搜尋 + Mini-Burst + Grid↔Detail 共享封面轉場 + Cover State Machine）+ GSAP Showcase 動效系統（Flip 排序 + stagger 分頁 + Motion Lab）+ Lightbox 重設計（glass circle overlay + metadata panel）+ Source Link 設定 + 安裝腳本升級 + 測試套件大整合（去重 + conftest 統一 + 覆蓋率補強）。測試數 731 → 1007。

## 0.3.x 系列 (v0.3.0 ~ v0.3.6, 2026-02-08 ~ 2026-02-20)

Bootstrap → DaisyUI + Alpine.js 全站遷移 + Showcase 動態化（SQLite SSR 取代靜態 iframe）+ Search Grid Mode + 女優資料卡（Graphis + JavBus 並行）+ Fluent Material Boost（Mica/Acrylic）+ GSAP 前置基礎建設 + Scraper 擴充（D2Pass/HEYZO/DMM/gfriends/Graphis）+ 路徑工具統一（uri_to_fs_path/pathToDisplay）+ UNC 路徑修正 + 版本標記覆蓋保護 + Jellyfin 圖片模式。測試數 523 → 817。

## 0.2.x 系列 (v0.2.0 ~ v0.2.4, 2026-01-22 ~ 2026-02-07)

Design System 建立（Fluent Design 2 視覺語言 + AV Card 4 變體 + Token 系統）+ macOS 支援（Alpha）+ AI 翻譯雙引擎（Ollama + Gemini）+ 多層目錄結構 + FC2/無碼搜尋 + SQLite 本地收藏庫 + Scraper 模組化架構（5 個獨立 scraper）+ 番號後綴清理修正。測試數 126 → 564。

## 0.1.x 系列 (v0.1.0 ~ v0.1.4, 2026-01-15 ~ 2026-01-17)

初始版本發布：多來源番號搜尋（JavBus/Jav321/JavDB）+ Gallery UI + 智慧搜尋 + 批次搜尋 + NFO 自動補全 + Settings（Dark Mode + Ollama 翻譯 + 輸出格式設定）+ 新手教學（Tutorial 4 步驟）+ Windows 打包 + 路徑工具統一（path_utils）+ 版本管理集中化。測試數 115 → 311。

> 完整歷史紀錄請見 [CHANGELOG_ARCHIVE.md](CHANGELOG_ARCHIVE.md)
