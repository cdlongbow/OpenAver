# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [0.13.4] - 2026-08-05

**如果你有接 Jellyfin／Emby／Kodi，這一版修好了封面牆顯示錯亂。** 從這版起新刮的片，封面牆會正確顯示直式海報，不再是一張橫式圖被硬塞進直式格子裡。**但舊片不會自動修好**——原因和唯一的修法寫在下面的「已知限制」，請務必看一眼。

### Changed
#### 🖼️ 媒體伺服器模式：封面改存成 `-fanart`，不再多存一張同名封面

- **問題是什麼**：接 Jellyfin／Emby／Kodi 時，每部片會產生三張圖——同名封面（橫式）、`-fanart`（橫式，和同名封面**一模一樣**）、`-poster`（直式）。這些軟體的封面牆是直式排版，本來該用 `-poster`，但**它們一律優先讀同名封面**，結果就是橫式圖被硬塞進直式格子。查證過 Emby 與 Jellyfin 都是這個行為，不是單一軟體的問題。
- **根因不是「少了什麼」，是「多了一個」**：只要同名封面**不存在**，這些軟體就會自動改用 `-poster`，畫面立刻正確。
- **改法**：媒體伺服器模式下**不再產生同名封面**，`-fanart` 直接成為封面的正式存放位置（它本來就是同名封面的完整複製，等於原本把同一張圖存了兩次）。`-poster` 完全不變。
- **順帶的好處**：每部新片**少存一份完整封面圖**。對動輒數千部的片庫是實在的空間節省。
- **對 Kodi 也有效**：Kodi 官方文件對「純同名 `.jpg` 算不算 poster」說法不一致，但改動後同名封面根本不存在，它只剩 `-poster` 可讀——而那正是正確答案。
- **「關閉」模式完全不受影響**：仍然只產同名封面，一個位元組都沒變。
- **OpenAver 自己的畫面也完全不變**：封面牆本來就是完整顯示橫式封面，讀的也是同一張底圖，只是換了個檔名存放。

### 已知限制
> **這三件事請一起看，它們是同一個取捨的三面。**

1. **舊片不會自動修好，重新刮削也不會。** 升級前已經產生的片，同名封面還在磁碟上、還是會被優先讀取，封面牆繼續顯示橫式。
2. **唯一的修法是你自己刪掉那些同名封面檔**（`-fanart` 和 `-poster` 留著就好）。本程式**不提供任何刪檔功能**，這是刻意的。
3. **為什麼不順手幫你改**：因為資料庫記錄的封面位置一旦被改動，OpenAver 會把它當成「你換了一張封面」——**你手動調過的封面對焦位置會被清空**、縮圖快取失效。用「舊片顯示維持現狀」換「不動你已經調好的東西」，是我們認為比較划算的取捨。所以：**這個改動只作用在本次真正首次產生封面的片**（全新入庫、或原本就沒有封面的片）。
4. **切換設定不需要重刮**：從媒體伺服器模式切回「關閉」（或反過來），既有的片照樣顯示得出來——記錄指向哪個檔，那個檔就還在。
5. 切回「關閉」之後，這些片的 NFO 裡的圖片欄位會指向一個不存在的檔名。**媒體伺服器會直接忽略這種欄位**（不破圖、不報錯），而「關閉」模式本來就沒有媒體伺服器在讀它，因此刻意不修。

### Fixed
#### 🖼️ 唯讀來源：輸出資料夾設在來源資料夾裡時，封面會被記成空白

- **誰會踩到**：把雲端／NAS 唯讀來源的「輸出資料夾」設在**來源資料夾自己底下**的人。
- **會發生什麼**：程式要把封面複製到正式位置時，來源和目的地剛好是**同一個檔案**，複製失敗後被當成「這片沒有封面」——資料庫記成空白、劇照與直式海報整段跳過，**但那張封面其實好端端地躺在原地**。你看到的是封面牆破圖。
- 這是**升級前就存在**的問題（本版讓它多了一條觸發路徑）。原始檔案全程沒有被動到，沒有資料遺失。

#### ⚠️ 設定值打壞時，一道防護會失效

- 「重新整理全部」這個動作有一道防護：如果這次操作**什麼檔案都不會寫**，就直接擋下來，避免資料庫和磁碟對不上。
- 但那道防護的判斷方式和實際寫檔的邏輯**用了兩套標準**——設定檔若被手動改壞（值打錯字、改成未知的值），防護會誤以為「這次會寫圖」而放行，實際上一張圖都不會寫，資料庫卻更新了。
- 現在兩邊統一成同一套判斷。這同樣是**升級前就存在**的問題。

### 測試
- 全套 pytest **5927 passed, 1 skipped**（v0.13.3 為 5873；本版新增 54 支）。`ruff check .`、`lint-imports`、函式規模閘、`npm run lint`、`npm test`（287）全綠。四條架構閘**零新增豁免**。
- 守衛總量三桶棘輪 **11,245 行，與 v0.13.3 完全相同**（本版純後端，未動前端／CSS 守衛涵蓋的檔案）。規模閘三支：`organize_file` **359 → 348**、`enrich_single` 249 不變、`_write_movie_assets` **218 → 214**（皆為淨負，靠「把內聯實作換成呼叫共用函式」換來的，不是申請豁免）。
- 來源金絲雀 **8 源全綠**——連三版紅的 avsox 本次恢復（站方自行修復，本版未觸碰任何 scraper 程式碼）。
- **翻面本身是「改一個函式、六處同時生效」**：上一版已經把全庫六個「封面該存哪」的推導點全部改成呼叫同一支共用函式，本版只換那支函式的本體。因此 review 只需要看「規則對不對」，不必再看「線接對了嗎」。
- **測試對齊靠一張真理表**：非唯讀 7 格 × 唯讀 9 格，逐格寫明「會不會重新下載／封面寫到哪／衍生圖產不產／資料庫記什麼／手動對焦會不會被清／NFO 三個標籤指向哪」。所有測試的斷言值都引用表格列號，不各自複述結論。
- **每一支新測試都做了 mutation 自驗，且逐次檢查紅燈的「形狀」**——本版有**四次**靠形狀抓到問題：① 一支測試的 fixture 讓被測的兩種寫法在數學上剛好等價，mutation 完全測不出差異（改成正確的磁碟狀態才測得到）② plan 預測「拿掉某道保護會讓某支測試轉紅」，實測維持綠——真正承重的是另一種錯誤路徑，補了專屬守衛才鎖得住 ③ 一個 mutation 第一輪 345 支全綠，追出原因是測試探的位置根本沒有殘留檔案 ④ 一條契約表的排除理由寫著一句**假的**前提，導致該格的守衛結構上不可能轉紅。
- **「既有測試維持綠 ≠ 行為沒變」這個形狀，本版命中七次**——每一次都追到機制層級（是哪個預設值、哪個 mock、哪個 fixture 讓測試從沒走到被改的分支）才放行，沒有一次停在「實測沒紅所以沒事」。
- **跨廠牌 review 計分卡**（兩段式）：
  - **Stage 1**：grok-4.5(high) 回 **LGTM 零 finding**（依既有規則計為零信號）；gemini 提 2、**採納 0**——一條的前提句與本專案的檔案慣例不符，另一條被 diff 視窗外的變數初始化直接推翻（純 diff reader 的已知盲區）。
  - **Stage 2**：codex 5.6-terra 判 **LGTM 零 finding**（但查證有實質內容：逐條核 AC、對真理表逐格、抽查五個讀取端）；**Opus(high) 提 1×P1 ＋ 2×P2 ＋ 3×P3，其中 P1 是另外三台都沒抓到的**——一條契約表的排除理由建立在假前提上，讓某個驗收條件的反向鎖結構上不可能轉紅。已補上真格子鎖住實際行為。
  - **PR review（Codex，Stage 之前）**：1×P1、採納 1（上面「設定值打壞時防護失效」那條）。
- **push 前 red-team（本版首次試行）**：攻擊 8 個宣稱、打破 1 個（上面「輸出資料夾設在來源資料夾裡」那條，已修）、弱破 1 個（措辭收窄）。它抓到的那條**是 Stage 1／Stage 2 四台 reviewer 全部漏掉的**，而且它是靠「寫可執行的攻擊腳本真跑」找到的，不是讀碼推理。成本約 45 分鐘。

## [0.13.3] - 2026-08-04

本版**九成是內部工程**（為下一版的封面改動先鋪地基，畫面與操作完全沒變），但過程中挖出**兩個今天就存在、而且會默默弄壞東西的 bug**——其中一個會直接把你的封面圖檔改小、且不可還原。**如果你有接 Jellyfin／Emby／Kodi，這一版建議升。**

### Fixed
#### 🖼️ 「補齊 Jellyfin 圖片」會把某些片的封面原圖就地裁小（不可還原）
- **誰會踩到**：你的片庫裡有那種**只有一張 `-poster` 圖、沒有同名封面**的影片。這種擺法是 MDCX、Javinizer 這類外部整理工具的常見產物，你自己不會特別察覺。
- **會發生什麼**：按下掃描頁的「補齊 Jellyfin 圖片」之後，程式會拿那張圖去裁一張直式海報，**而「來源」和「目的地」剛好是同一個檔案**——結果就是直接把你的原圖裁小蓋回去（實測 800×538 變成 379×538），而且介面上還顯示「成功」。**那張被裁小的圖，正是封面牆和燈箱實際顯示的那一張。**
- 這違反了本專案一條基本原則：**衍生出來的圖片，永遠不該回頭覆蓋原始檔案**。
- **另一種情況也修了**：如果 `-poster` / `-fanart` 是用「捷徑」方式指向同一張圖（hardlink／symlink，某些工具會這樣省空間），同樣會踩到，而且**光看檔名看不出來**。現在會實際判斷「這兩個路徑是不是同一個檔」，不只是比對檔名。
- **已經被裁小的圖沒辦法自動救回**（原始內容已經沒了）。若你在意，可以對那幾部片重新刮削封面。

#### 🗂️ 部分影片被誤判成「缺 NFO」
- **誰會踩到**：從外部媒體庫掃描進來、封面被記成 `<片名>-fanart.jpg` 的影片。
- **會發生什麼**：程式在回填「NFO 什麼時候更新過」時，會去找一個**永遠不存在的檔名**，於是整批放棄——這些片在瀏覽頁就會一直顯示成缺 NFO 的狀態，即使 NFO 明明就在旁邊。無錯誤訊息、無日誌，純靜默。
- 現在會先正確去掉 `-fanart` / `-poster` 後綴再找，這些片下次啟動就會恢復正常。

#### 🗂️ 補圖時偶爾出現的「fanart 複製失敗」假警告
- 封面本身就叫 `<片名>-fanart.jpg` 時，程式會試圖把它複製成它自己，然後把這個必然失敗記成一行紅字。實際上什麼都沒壞。現在會正確辨識成「這張已經到位了」。

### Internal
#### 🔧 封面「該存在哪個檔名」收斂成單一決策點
- 全庫原本有 **6 個地方各自推導**「封面要寫到哪、資料庫要記哪個路徑」，改一個地方只有一條路徑會生效——這正是下一版要動封面命名時最大的漏改風險。本版把它們全部改成呼叫同一支共用函式。
- **本版刻意讓那支函式維持原本的答案**（永遠回同名 `.jpg`），所以這一步是**純代換、零行為變更**——證明方式是「接線那個 commit 完全沒有修改任何既有測試檔，而全套測試照樣通過」。真正的規則變更留到下一版，那時 review 只需要看「規則對不對」，不必再看「線接對了嗎」。
- 同時新增一組「契約表」測試（26 種情境），從三個對外入口驗證最終產出的檔案、NFO 內容與資料庫記錄，作為下一版改動的安全網。

### 已知限制
- 影片檔名本身就以 `-fanart` 或 `-poster` 結尾時（例如 `my-fanart.mp4`），上面 NFO 那條修正會誤判而跳過該片。實務上片名不會長這樣（檔名由番號／標題／女優組成），且修掉它會破壞另一項相容性保證，因此刻意保留。

### 測試
- 全套 pytest **5873 passed, 1 skipped**（v0.13.2 為 5775；本版新增 98 支）。`ruff check .`、`lint-imports`、函式規模閘、`npm run lint`（static_guard 1045 條）全綠。四條架構閘**零新增豁免**。
- 守衛總量三桶棘輪 **11,245 行，與 v0.13.2 完全相同**（本版純後端，未動前端／CSS 守衛涵蓋的檔案）。
- **每一支新測試都做了 mutation 自驗**，且**逐次檢查紅燈的「形狀」對不對**——不是「有紅就算過」。本版有三次靠形狀抓到問題：① 拿掉某個 `break` 竟然 0 支轉紅（代表 10 格邊界表沒有一格能讓那行程式碼產生差異，補了反向排列才釘住）② 契約表最大那張表刪掉一整格，覆蓋守衛竟然全綠（守衛用「白名單反推」自動吸收了缺口，改成硬編碼清單後才會紅）③ 兩處「檔案不被覆寫」的斷言是拿檔案跟自己比，恆真。
- 來源金絲雀 **7 通過／1 失敗（avsox）**：站方回空內容，屬外部改版形狀，與本版改動無關（本版未觸碰任何 scraper 程式碼），依既有 triage 流程觀望、另案處理。
- **跨廠牌 review 計分卡**：本版首次採用「Stage 1 便宜先掃 → Stage 2 主力」的兩段式。
  - **Stage 1**：gemini 提 3、採納 1（fail-closed 分支缺日誌）；grok 前兩次因上游 HTTP 521 中斷，第三次成功、回 **LGTM 零 finding**（依既有規則 LGTM 計為零信號）——但它獨立點出了下一版的一個已知地雷，與我方紀錄同源。
  - **Stage 2**：codex 判 **LGTM 零 finding**；Opus（high）判 **FAIL**，提 1 個 P1 ＋ 5 個 P2 ＋ 7 個 P3，**全部經獨立重現後採納**。六條主要條目裡**有五條純讀 diff 讀不出來**，必須追呼叫端或跨檔對帳才看得見——這是兩段式結構第一次拿到這麼明確的對照。
  - Opus 抓到的 P1 是**本 branch 自己引入的回歸**：安全判斷（不覆寫原檔）與成功回報用了同一個布林值，導致某些 I/O 錯誤下「什麼都沒寫卻回報成功」，進而讓 NFO 寫出指向不存在檔案的標籤、並在改標題時刪掉舊圖留下空洞。已拆成兩個獨立訊號修正。
- **PR review（Codex）另提 3 輪、共 3 條 P1，全部採納**：同檔判斷只比字串擋不住 hardlink／symlink；例外分類把「檔案不存在」和「未知錯誤」混為一談；以及「靠複製函式自己報同檔」不足以取代事前判斷（複製函式內部會把未知錯誤吞掉並照常寫入，對同一個檔案就是清空它）。三條都是**先重現、再修**。

## [0.13.2] - 2026-08-03

本版兩件事：**①「外部媒體管理器」設為「關閉」時，雲端／NAS 唯讀來源不再多產出兩張沒人看的圖**（對絕大多數使用者是無感的空間節省；畫面、播放、縮圖、對焦全部不變）；**② 修好 HEYZO 搜尋完全查無的問題**（站方改版造成，見下方 Fixed）。

### Changed
#### 🗂️ 唯讀來源在「關閉」模式下不再產生 `-poster` 與 `-fanart`
- 「外部媒體管理器」這個設定有四個選項：**關閉** / Jellyfin / Emby / Kodi。它的用途本來就只是「要不要順便產出給媒體伺服器看的圖」。
- 但過去**唯讀來源**（雲端／NAS 分享盤那種只能讀不能寫的來源）不管你選哪個，**一律都會多產出 `-poster` 和 `-fanart` 兩張圖**——而一般（可寫入）來源選「關閉」時本來就只產同名封面。同一個設定，兩種來源行為不一致。
- 現在對齊了：**選「關閉」時，唯讀來源也只產同名封面。**
- **這兩張圖在「關閉」模式下確實沒有任何讀者**（已逐條查證）：OpenAver 自己挑封面時，同名封面的優先序永遠排在它們前面；而「關閉」的意思就是你沒有接 Jellyfin/Emby/Kodi，不會有外部程式來掃這個資料夾。
- **省多少**：`-fanart` 是同名封面的完整複製（位元組一模一樣），`-poster` 是另一張直式裁切圖。等於每部片少佔約兩張封面的空間——對動輒數千部的唯讀網盤庫，是實在的節省。
- **選 Jellyfin / Emby / Kodi 的人完全不受影響**，三張圖照舊產生。

#### 🗂️ 「Jellyfin 圖片」補齊功能同步尊重這個設定
- 掃描頁那個「補齊 Jellyfin 圖片」的功能，過去**只在畫面上依設定隱藏按鈕，後端本身不檢查**。也就是說設定為「關閉」時，它其實仍有辦法被觸發、把剛剛省下來的兩張圖又補回去。
- 現在這道判斷移到**後端**：設為「關閉」時，它一律回報 0 筆待補、**一個檔案都不會寫**。選 Jellyfin/Emby/Kodi 時一切照舊。
- 也修掉兩個時序問題：① 剛切換完設定的 60 秒內，待補數量可能顯示成舊的數字；② 批次補圖跑到一半時若把設定改成「關閉」，剩下的影片仍會繼續被補圖——現在會在下一部片就停住。
- **設定值不合法時一律當成「關閉」處理**：設定檔若被手動改壞（值打錯大小寫、留成空值、或改成未知的值），過去圖片會被擋下、但 NFO 裡仍會寫入只有媒體管理器該看的欄位（`<lockdata>` 等），等於只擋了一半。現在這種值會統一收斂成「關閉」，圖片與 NFO 兩邊行為一致。合法的四個選項完全不受影響。

### Fixed
#### 🔍 HEYZO 搜尋整個查無 —— 站方改版造成，已修復
- **症狀**：搜尋任何 HEYZO 番號都是「查無結果」，而且**完全沒有錯誤訊息**，看起來就像那部片不存在。
- **原因**：HEYZO 網站改了做法——影片資料以前是直接寫在網頁裡，改成由網頁上的程式在瀏覽器打開後才組出來。OpenAver 是直接讀網頁原始內容的，讀不到那段資料就整個放棄了。（資料其實還在網頁裡，只是換了個形式擺放。）
- **修法**：兩種形式都認。舊形式優先，讀不到就改讀新形式。**兩種都讀不到時會留下一行明確的紀錄**，以後這個站再變花樣，一看日誌就知道是網站變了、而不是又要從頭猜。
- **順手補回三個被同一次改版弄丟的欄位**：
  - **女優名**：網站在新形式裡把女優欄位留成空白，改成從頁面上的資料表讀（跟你在網頁上看到的那格是同一個）。
  - **發售日**：新形式裡那個日期欄位的值已經不對了（有片子寫成 2018 年，實際是 2015 年），改讀資料表裡的「Released」。未發售的新片顯示「Coming Soon」時留空、配信期間寫成一段區間時取開始那天。
  - **系列名**：網站把系列名加上了連結，舊的讀法會讀成空白。像《Sex Heaven》《After 6》《Asi-Hame!!》這些系列名現在都回來了。
- **順帶效果**：HEYZO 的封面、片長、劇照、評分本來就沒壞，但因為整支搜尋都失敗所以一併拿不到——現在全部恢復。

### 已知限制
- **舊的檔案不會自動刪除**：升級前已經產生的 `-poster` / `-fanart` 會留在磁碟上。它們不影響任何功能（本來就沒有讀者），只是繼續佔空間，可自行刪除。
- 若某部片在升級後**改了標題**再重刮，同一個資料夾裡會同時留著舊標題的那兩張殘留圖，情況同上。

### 測試
- 全套 pytest **5775 passed, 1 skipped**（v0.13.1 為 5729；本版新增 46 支＝唯讀輸出對齊 25 支＋HEYZO 修復 21 支）。`ruff check .`、`lint-imports`、函式規模閘、`npm run lint`、`npm test`（287）全綠。
- 新增測試涵蓋：四格矩陣（唯讀 × 關閉／三種媒體伺服器風味各一格）、NFO 圖片標籤不留懸空引用（含無封面邊界）、ingest 自帶圖片不被複製、來源磁碟零寫入、以及補齊端點的 fail-closed 邊界（設定值為空／缺欄位／大小寫不符／未知舊值一律當成「關閉」）。
- **每一支新測試都做了 mutation 自驗**：把改動還原後確認該測試單獨轉紅，反向鎖（媒體伺服器情境）維持綠——避免寫出「永遠會過」的空殼測試。
- **真機驗收**：在隔離環境真起服務、真產出一份「關閉」模式的唯讀庫，用瀏覽器逐項確認 spec 點名的 7 個介面（封面牆／片單卡片／燈箱／縮圖／焦點裁切／相似探索／匯出 HTML）全部顯示正常、147 筆請求零錯誤、零 console error。並確認兩種模式的產物各自落在正確的輸出夾。
- 守衛總量三桶棘輪 **11,245 行，與 v0.13.1 完全相同**（本版純後端，未動前端／CSS 守衛涵蓋的檔案）。四條架構閘**零新增豁免**。
- 來源金絲雀 **8 通過／0 失敗**：連三版紅的 heyzo 已於本版修復（見 Fixed），八個來源首次全綠。
- **HEYZO 修復的驗收**：新測試以真實抓回的網站頁面為底（不是手搭的假頁面），涵蓋 16 種邊界（新形式缺失／半毀、女優欄缺失或多人、未發售片、日期區間、沒有系列欄的新片、404、逾時等）；每一支都做 mutation 自驗（把修法還原後該測試須單獨轉紅）。另真起服務打真 API，對站上當前看得到的 9 個番號（含最新片）逐一確認查得到、封面與標題齊全。
- **grok reviewer 恢復可用，並在本版抓到一條真 finding**：先前連三版記為「缺 bubblewrap 無法啟動」，實為本機未安裝 `bwrap`（grok 的唯讀沙箱在 Linux 只能靠它；事件日誌顯示其中一次的舊失敗其實是另一個原因，兩者先前被歸成同一類）。裝好後首次執行即指出：新解析路徑用 `re.sub` 的**字串** replacement 插入 `json.dumps` 結果，遇非 ASCII 會拋 `bad escape \u`、遇反斜線會生出非法 JSON，兩者都被 except 吞掉 → 整個來源再次靜默查無。站方目前把該欄位留空所以踩不到，但那是站方的實作細節、不是我方保證——已改用 lambda replacement 並補兩支回歸鎖（含 mutation 自驗）。
- **Pre-merge 跨廠牌 review 計分卡**：codex 提 2（皆 P2：TTL 快取讓後端判斷被繞過、批次補圖中途改設定的時序漏洞）、**採納 2**；Opus（high）提 5（皆 P3，含前者的反向情境：關閉模式的 0 被快取後、切回媒體伺服器模式會有 60 秒假 0）、採納 3（其一與 codex 那條同根因、一次修掉）。grok 因本機缺少沙箱工具（bubblewrap）無法啟動，本輪未執行、不列入計分（連續第三支）。
- **PR review（Codex）另提 1 條 P2、採納**：設定值不合法時只 fail-closed 了一半（圖片擋住、NFO 仍寫媒體管理器欄位）。這是本版把「圖片無條件產生」改成「白名單產生」時新引入的子情境——已於設定讀取處統一收斂。**同型的舊洞在非唯讀路徑（`organizer.py` / `enricher.py`）自 v0.9.11 起就存在、本版未觸碰該處程式碼**，已列具名 backlog 另案處理。

## [0.13.1] - 2026-08-02

本版是**安全修補 + 內部治理**（feature/110），分兩段。對一般使用者來說：**畫面和操作完全沒變，但底下的安全性補了三個洞、換掉一批有已知漏洞的元件**。唯一會被察覺的是最後那條「開發模式啟動命令」的變更，而且只影響在 Linux 上從源碼跑的人。

### Security
#### 🔒 換掉兩個有已知漏洞的元件（一次消掉 17 個 CVE）
- **影像處理元件（pillow）升到 12.3.0**，一次補掉 **13 個** 已公開的漏洞。其中幾個是真的會踩壞記憶體的類型——裁圖／貼圖／合成時的座標溢位、色彩管理轉換時的記憶體破壞、濾鏡的整數溢位、以及一個會把約 57KB 相鄰記憶體內容寫進輸出圖檔的洩漏；其餘是「解壓縮炸彈」防護被繞過、幾種圖檔格式的越界讀取與無窮迴圈。
  **為什麼這對 OpenAver 是真的有影響**：封面／劇照是從外部網站下載後直接落地，接著就交給這個元件去裁圖、產縮圖、做人臉對焦——中間沒有任何格式檢查。也就是說，一個被動過手腳的封面圖檔，原本是有機會走到那些漏洞裡的。升版後這條路徑上的已知漏洞都沒了。
- **檔案上傳解析元件（python-multipart）升到 0.0.32**，補掉 **4 個** 漏洞，其中一個是「送一個負數的長度欄位就能讓整包資料塞進記憶體」。這個元件在 OpenAver 只服務女優照片上傳一個地方。
- 兩者都釘在目前的最新修補版。其餘 19 個外部元件逐一查過，沒有已知未修補的漏洞，因此不動。

#### 🔒 檔案整理／唯讀庫產出加上「不准寫到根目錄外面」的最終防線
- 整理檔案與生成唯讀媒體庫時，資料夾名稱是用刮削回來的資料（女優、片商、標題、年份）組出來的。過去**組完之後從來沒有驗證過「這條路徑是不是還在你指定的資料夾底下」**——如果某個來源網站回傳的女優欄位剛好是 `..`，多層資料夾組起來就會一路往上跳，檔案可能被搬到你指定範圍之外。
- 現在在**真正建立資料夾與搬移檔案之前**加了一道檢查：路徑先完整解析（包含解開捷徑／symlink、處理 `..`），確認仍在根目錄底下才放行，不在就中止並回報錯誤、**一個檔案都不動**。
- **不會誤殺正常片名**：這道檢查看的是「最後算出來的位置」，不是「名字裡有沒有 `..`」。像 `Vol.2`、`A.B.C` 這種含點的片名照樣正常整理。
- 遇到跨磁碟機、網路磁碟機無法解析等狀況一律**保守拒絕**，不會因為判斷不出來就放行。

#### 🔒 「檢查更新」按鈕加上三道防護
- Help 頁的「更新」按鈕背後那支功能，會開一個終端機視窗下載並執行安裝腳本。過去它只檢查「你是不是在用桌面版」，**沒有檢查請求是從哪裡來的**——這代表你在瀏覽器開到的任何一個惡意網頁，都可以在背景偷偷替你按下這顆按鈕。
- 現在要**三個條件同時成立**才會執行：① 必須是桌面版**而且**請求來自本機 ② 若請求帶了來源網站資訊，那個來源必須是本機 ③ 必須帶一個只有 OpenAver 自己前端會加的識別標頭（惡意網頁的表單送不出這種標頭）。任一條不過就直接拒絕，**而且是在啟動安裝程序之前就拒絕**。
- 同時關掉另一個暴露面：開啟「伺服器模式」讓手機連進來時，過去**同一個區網內的任何裝置都能觸發你電腦跑安裝腳本**；現在這支功能一律只認本機。

### Changed
#### ⚠️ 開發模式啟動命令改為只綁本機（行為變更）
- `run.sh` 與 README／CONTRIBUTING 教的開發啟動命令，從 `--host 0.0.0.0`（監聽所有網路介面）改為 `--host 127.0.0.1`（只有本機連得到），與 `SECURITY.md` 一直以來寫的「預設綁定 `127.0.0.1:8000`」對齊——先前是文件與實際行為不一致。
- **誰會受影響**：只有從原始碼用 `run.sh`（開發模式）啟動、並且靠它讓手機或其他電腦連進來的人。**一般使用者用的桌面版完全不受影響**（本來就只綁本機，而且有正式的「伺服器」開關）。
- **要繼續從其他裝置連進來**：開發模式請自己明確加上 `--host 0.0.0.0` 再啟動，並知道那會把服務暴露在所有網路介面上（本程式不設帳號密碼，僅限完全信任的網路）。差別是「預設安全、要暴露得自己明講」，不是「不准暴露」。
  > ⚠️ **不要以為可以改用設定頁的「伺服器」膠囊代替**——那顆開關背後的區網 listener 只在**打包桌面版**的啟動流程裡被接線，`run.sh` 這種直接跑 `uvicorn` 的程序切過去會回「無法啟動 LAN 伺服器」。這是 `run.sh` 一直以來就有的限制（不是本版造成的），但本版把預設綁定收窄後它才變得要緊，所以在此講明。桌面版的「伺服器」模式一切照舊。

### Internal
#### 🔧 四條「債長不回來」的機械閘上線
本版把四件「目前剛好乾淨、但沒有任何東西保證它繼續乾淨」的事，變成 CI 會擋的檢查。**它們只擋新增或惡化，不強迫把現有的東西全部改乾淨**——現存超標的項目逐條具名豁免、附上理由，而且豁免清單**只准減不准增**。
- **分層契約**：凍結「核心模組不得反向依賴 web 層」這個已經成立的事實（目前 0 違規）。
- **圈複雜度上限**與**函式行數上限（200 行）**：兩條互補的規模閘。後者是自己寫的掃描腳本——因為現成工具數的是「敘述句數量」，對「一個函式塞 1000 行字串」這種形狀完全無感（本專案真的有 4 個這種函式）。
- **模組邊界**：擋「跨模組去拿別人的私有成員」。
- 規模閘還會**反過來檢查豁免清單本身**：某條豁免的函式如果已經縮到門檻以下、或根本已經不存在，腳本會直接報錯要求把那條豁免刪掉。**清單只准減，由機器執行，不靠自律。**

#### 🔧 測試「還改不改得動」的觀測（milestone 級，不擋任何 PR）
- 新增 milestone 級的全庫架構掃描，記錄「哪些名字被測試綁死最多次」的排行、四條閘的豁免條目數、以及重複程式碼的參考掃描。
- **刻意不設任何測試數量或行數上限**——要量的不是測試有多少，是「這塊碼還改不改得動」。

### 測試
- 全套 pytest **5729 passed, 1 skipped**（v0.13.0 為 5655；本版新增 74 支，涵蓋路徑防線的 11 種邊界、整理與唯讀產出的逃逸與不誤殺、更新按鈕的 11 個具名情境與兩支輔助函式真值表，以及 CI 釘版守衛的 19 種命令形狀）。`ruff check .`、`lint-imports`、函式規模閘、`npm run lint`、`npm test`（287）全綠。
- **四條閘的豁免條目數：C901 7 條／規模 14 條／模組邊界 12 條，全 branch 零新增**——本版寫的每一行新程式碼都活在閘底下且沒有申請任何豁免。
- **真服務端到端驗收**：真起服務（非測試替身）對「檢查更新」端點打 **11 個具名情境**，逐格記錄實際回應碼，全數符合預期；其中「外站來源」與「區網來源＋伺服器模式開啟」兩格是唯一能證明對應那層防護真的存在的格子。並以攔截記錄坐實「放行的 3 格真的會去執行安裝、被拒的 9 格零副作用」。使用者真實設定檔全程未被觸碰（雜湊前後相同）。
- **真檔案驗收**：用真實檔案跑整理，逃逸情境下「原始檔仍在原位、整個沙箱目錄前後快照完全相同」；合法片名（含點、含被清理過的斜線）照常整理完成。
- 守衛總量三桶棘輪 **11,245 行**（上限 11,225，+20）：**這 +20 全部是同一條 lint 規則**，鎖住「更新按鈕的識別標頭不得從前端被移除」——那個標頭若被誰順手清掉，按鈕會**靜默失效**（使用者只看到一個錯誤提示）。+11 是規則本身；另 +9 是把它的掃描範圍從「整個檔案裡有沒有這個字」收窄到「這個字必須真的在送出請求的那個函式裡」（review 指出前者是假保護：把標頭搬進註解或無關函式都能維持全綠，但按鈕實際上已經壞了）。這正是 lint 守衛該存在的形狀。
- 來源金絲雀 **7 通過／1 失敗（heyzo）**：已依既有 triage 流程確認為站方把頁面 toggle 回純 JS 動態載入（實測靜態結構化資料 tag 消失、`movie_obj` 仍在），屬外部網站行為、與本版改動無關，沿用 v0.13.0 的處置另案處理。
- **Pre-merge 跨廠牌 review 計分卡**：Opus（high）提 2 條（皆中等風險，且都是「隱含依賴沒寫進註解」而非活漏洞）、採納 2；codex 提 1（路徑檢查的競態窗口）、**不採納**——它需要的攻擊者能力（在你本機與整理同時建立捷徑）已超出專案宣告的威脅模型，且有那種能力的攻擊者根本不需要繞過這道檢查；已列具名 backlog 並在程式碼註解留痕。grok 因本機環境缺少沙箱工具無法啟動，本輪未執行、不列入計分。
- **Review 在合併前攔下三條開發期問題**（皆本 branch 內引入、從未進入任何已發佈版本）：① 唯讀產出的防線第一版寫成「沒傳參數就整條失效」的形狀，已改為無條件檢查；② 判斷「是不是本機」時漏掉一種 IPv6 表示法，會在某些網路設定下**把桌面版自己鎖在門外**；③ 一種畸形的來源網址會讓伺服器回 500 而非預期的拒絕。三者皆已於合併前修正。

## [0.13.0] - 2026-08-01

本版是一次純內部收斂（feature/109）：把唯讀來源「單片刮削」與「批次補料」兩處各約 40 行、幾乎一模一樣的產出邏輯（決定要沿用本地資料還是重新刮削、實際寫檔、失敗時回什麼形狀）收斂成單一入口、兩邊共用。**對使用者完全無感、零行為變更**——這版不新增功能，純粹是把「同一條規則要記得改兩個地方」的維護債消掉。

### Internal
- **唯讀單片 enrich 的產出核心收斂為單一入口**：新增 `core/readonly_producer.py` 的公開入口 `enrich_one_readonly`，原本分別寫在「單片刮削」與「批次補料」兩處端點裡的重複編排（沿用本地資料或重新刮削的判斷、實際寫檔、封面與焦點記帳）改為呼叫同一份實作。
- **產出失敗有了統一的型別化邊界**：新增 `ReadonlyProduceError`，讓批次補料在唯讀片產出失敗時，維持既有的「生成失敗」錯誤文案與行為，不因為改走共用入口而變樣。
- 三處刻意保留在各自呼叫端、不強行收斂：javlibrary 指定版本預抓（單片限定）、部分早退檢查與輸出路徑解析（兩端順序刻意相反）、封面縮圖快取失效時機（兩端不同）——這幾點兩端語意本來就不同，強行合併只會改變其中一端的行為。補劇照功能維持獨立實作，不在本次收斂範圍內。
- 順手清掉因收斂而不再使用的程式碼（未使用的 import、重複的欄位摘要邏輯）。

### 測試
- 全套 pytest **5655 passed, 1 skipped**（unit + integration，排除 smoke／e2e）。`ruff check .` 全綠、`npm run lint` 全綠（static_guard 1044／css-guard／cjk 皆 clean）。
- 新增 AST 結構守衛，機械鎖住「單片與批次都必須走同一份收斂後的產出邏輯、不得各自繞路手抄」，並逐條 mutation 自驗每條守衛能各自單獨抓到對應的破壞方式。
- 守衛總量三桶棘輪 **11,225 行，與 v0.12.10 完全相同**（本版純後端收斂，未動前端／CSS 守衛涵蓋的檔案）。
- 來源金絲雀 **7 通過／1 失敗（heyzo）**：確認 heyzo 官網把頁面改成純 JS 動態載入（不再提供靜態結構化資料），屬外部網站改版，與本版改動無關，另案處理。
- 額外在隔離環境真起服務、真打單片與批次兩條 API 端點驗證：確認來源磁碟全程零寫入（前後遞迴快照逐項相同）、產出檔案正確落地、進度通知序列完整。
- **Pre-merge 跨廠牌 review 計分卡**：codex 提 1（中等風險，批次補料的封面對焦排序在收斂過程中被打亂——已於合併前修正，見下）、採納 1；另一顆 Opus（high）提 1（中等風險，四處測試斷言因程式搬移後失去效力、變成沒在驗證任何東西的空殼）、採納 1 + 3 條低風險不阻擋；grok 因本機環境缺少沙箱工具無法啟動，本輪未能執行、不列入計分。
- **Review 在合併前攔下兩條開發期回歸**（皆本 branch 收斂過程中引入、從未進入任何已發佈版本，對既有使用者無影響）：① 唯讀單片重刮，寫檔完成後若最終檢查失敗會漏一次縮圖快取失效，導致舊封面縮圖卡住不消失；② 批次補料，同樣失敗情境下對焦排程時序被打亂，新封面會誤用舊封面的手動對焦座標。兩者皆已於合併前修正，未影響任何已發佈版本的使用者。

## 0.12.x 系列 (v0.12.0 ~ v0.12.10, 2026-07-14 ~ 2026-07-24)

- **焦點裁切上線與收尾**（v0.12.0～v0.12.1、v0.12.3，feature/98/99/101）：無碼封面（FC2／素人／uncensored）刮削或掃描時自動偵測人臉、以臉為中心裁切封面與燈箱大圖，有碼片維持原樣零成本；燈箱補上「自動對焦→左右拖曳微調→存檔」正式互動、唯讀來源也納入自動對焦、牆上小格裁切改與燈箱大圖同精準度；後續再擴及 Jellyfin/Emby/Kodi 媒體伺服器海報，對焦等待動畫改視覺連續交棒，影片對焦鈕改為只在窄螢幕／手機才出現。
- **女優照片管理更完整**（v0.12.2、v0.12.4，feature/100/102）：女優頭像可自行上傳照片、也能像影片一樣手動對焦裁切；修好改過名（有別名）的女優在照片挑選視窗選本機候選必失敗、雲端重抓永遠查不到圖的問題，重抓改為自動輪流用她的每個名字查詢；順手讓搜尋詳情、燈箱、劇照瀏覽都能用滑鼠滾輪切換上一片／下一片。
- **技術債清償**（v0.12.5，feature/103）：約 130 行硬編碼中文文字收進語言檔；拖曳檔案與貼上檔名的番號辨識規則統一為同一套後端規則（不再兩套邏輯漂移）、辨識失敗改明確提示；清除死碼、logger 與中文硬編碼機械 lint 守衛正式上線。
- **唯讀來源（雲端／NAS 分享盤）功能大幅強化**（v0.12.6、v0.12.7，feature/104/105）：已經整理好的唯讀庫（旁邊有 `.nfo`／封面）改為直接就地讀用、不再無腦重刮，唯讀片的放大鏡補料／齒輪重刮／補劇照三顆鈕全面解禁（產物仍一律落獨立輸出夾、來源零寫入）；並把散在四處、常常改一處漏改一處的「記帳與回報」邏輯收斂成一份共用實作，順手修好唯讀破圖、原文標題被清空等一批連帶 bug。
- **整理前手動修正 + 桌面體驗收尾**（v0.12.8～v0.12.10，feature/106/107/108）：拖檔進搜尋頁後可在整理前直接改演員名單、幫無碼片補發售日；桌面版加上開機自動檢查更新（可關）、修好深色主題幾顆看不清的按鈕；手機瀏覽封面吃滿寬度、觸控裝置封面不再擠一排操作圖示（改進燈箱操作）、女優牆卡片大小對齊影片牆不再忽大忽小。

測試數 5233 → 5640（0.12.0 全文未附測試段落，範圍取最早可查的 0.12.1 起；前一系列 0.11.12 收尾值為 5030）。

## 0.11.x 系列 (v0.11.0 ~ v0.11.12, 2026-06-27 ~ 2026-07-12)

- **v0.11.0**：JavBus 過度泛用清償 + exact 番號搜尋改優先序 cascade（feature/85）——直接搜番號改為依你拖曳的來源優先順序逐一查詢（cascade，命中即回），JavBus 不再無視優先序搶先短路；DMM proxy 透傳修復、前綴搜尋 `type` 參數修正；拔除已死的 JavBus variant（同番號多版本）探查死碼。
- **v0.11.1**：JavLibrary 同番號多版本手動切換（feature/86）——搜尋框直搜／燈箱換來源／結果卡替換來源三入口皆可看封面手動挑撞號版本（游標預設停最新發行日），桌面 standalone 限定（需 CF transport）。
- **v0.11.2**：`core/database.py` 模組化拆分（feature/87）——2,152 行單檔拆成 `core/database/` 套件（六個領域子模組 + 永久 re-export facade），消除 `AliasRepository`／`TagAliasRepository` 鏡像重複碼（共用泛型基類），零行為／API／schema 變更。
- **v0.11.3**：唯讀來源生成本地媒體庫「off 風味」首發（feature/88）——scanner 來源可勾「唯讀」＋設輸出夾，來源零寫入下生成每片一資料夾的本地庫（NFO + 封面 + 劇照）並直接寫進 DB，供 OpenAver 自身瀏覽／串流播放雲端原檔；給 Emby/Jellyfin/Kodi 的 media-server（`.strm`）風味延後至下一版（feature/89）。

- **v0.11.4**：唯讀產生庫「地基」+ 掃描頁「試過」記憶 + 來源刪檔清死卡（feature/89）——生成片記住輸出夾（`videos.output_dir`）重刮原地更新不長重複夾、off 風味固定輸出夾免設定；試過／已生成的片不再被「缺資料」嘮叨、刮不到只試一次；唯讀網盤掉線明確警告不誤報成功；來源刪檔後 DB-row-only 清死卡（零檔案刪除）。
- **v0.11.5**：唯讀來源生成媒體伺服器庫「.strm 風味」+ 跨機器路徑映射 + 唯讀寫入全面封鎖（feature/90）——唯讀來源可產出 `.strm` 捷徑檔給 Emby/Jellyfin/Kodi 掃描播放，跨機器「播放端路徑替換」規則讓 `.strm` 內路徑翻成播放端看得懂的形式、改規則一鍵同步改寫既有 `.strm`；同時把唯讀來源「零寫入」補到滴水不漏（勾唯讀破壞性確認、四個寫入入口全面停用、切模式清舊媒體卡、產生中斷乾淨收尾）。
- **v0.11.6**：跨機器路徑映射（WSL2+UNC）讀寫全棧收斂 + DB-key 命名空間守衛（feature/91）——純 correctness 重構：修好「讀取端忘了在碰磁碟前把映射路徑反解回本機路徑」導致縮圖／封面／串流跨機器讀不到的一類 silent bug，以及「已反解路徑又被裸餵回 DB key」導致重刮掉使用者標籤、女優照 403 的另一類；兩支 AST 結構守衛擋死回歸。

- **v0.11.7**：搜尋頁體驗優化（feature/92）——搜尋列右側控制項不再擠壓變形、整理入口常駐可見更好找、整理入庫「飛入側邊欄」動畫加強為三段節奏，並抽成可跨頁複用的共用元件；順手修好「重新整理已有結果頁面時出現兩組 ✕」的 latent bug。
- **v0.11.8**：各刮削來源評分/簡介補進 NFO（feature/93，NFO-only）——七源評分、六源簡介寫進 `<rating>`/`<plot>`，純服務媒體伺服器（Jellyfin/Emby/Kodi）用戶，OpenAver 自身介面不顯示；順手美化燈箱中繼資訊視覺層次。
- **v0.11.9**：掃描頁「補資料」升級成逐片進度卡 + 命中封面飛入圖書館（feature/94）——補資料時逐片可視進度（搜尋中／命中／查無／失敗／唯讀跳過），真封面命中飛入側邊欄「瀏覽」入口。
- **v0.11.10**：設定頁命名區膠囊化 + 列表生成兩層 IA 重排（feature/95）——檔案命名格式從手打字串改成「變數膠囊 + 字面文字」視覺化編輯器、資料夾層級改動態清單（硬上限 3 層）；列表生成設定拆成日常常用（常駐）+ 離線 HTML 匯出（摺疊進階）。
- **v0.11.11**：前端靜態守衛 pytest → lint 全面遷移（feature/96，test-deflation）——純內部工程里程碑（零產品碼、對使用者隱形）：把 `test_frontend_lint.py` 裡「讀原始碼做字串/結構斷言」的守衛搬回工具層（新增 `i18n_lint`／`static_guard_lint`／`css-guard` 三支 `.mjs` + eslint `SEL_*` 家族），`test_frontend_lint.py` 16,749→5,041 行（−70%）、36 個真 contract class relocate 進 `frontend_contracts/`；north-star＝「能用 lint 機械處理的不進 pytest、不耗 Codex 審」。
- **v0.11.12**：javdb 在 released 版復活（feature/97）——打包瘦身刪除所有 `.dist-info` 導致 curl_cffi import-time 拋 `PackageNotFoundError`、被靜默吞掉，released Windows/macOS 版 javdb 從第一天就零 HTTP 請求；不再剝除 dist-info 即修復，並補打包產物 runtime 驗證守衛（`verify_artifact_imports.py` + dist-info 靜態檢查 + CI verify job）堵死 dev-only 測試盲區；本版另 bundle 兩個後續修復：非 ASCII 安裝路徑 `curl error 77`（CAINFO 改用 ANSI code page 編碼）+ 番號 7 字母前綴拖檔截斷修復。

測試數 4735 → 5523（v0.11.10）→ 4985（v0.11.11 test-deflation：守衛遷 lint 層等價承接，−538 非覆蓋損失）→ 5030（v0.11.12，+45：打包產物驗證守衛 + javdb CAINFO + 番號 cap 對齊回歸鎖）。

## 0.10.x 系列 (v0.10.0 ~ v0.10.11, 2026-06-18 ~ 2026-06-24)

- **來源穩定性 + 測試硬化**（v0.10.0，feature/73）：8 源真實番號健康金絲雀 smoke（三態 quorum 判讀）+ avsox 復活（站方轉 SPA 後改打背後 JSON API）+ Tokyo Hot 單字母無碼番號查無修復 + 覆蓋率地板 84%（`cov-floor`）+ 五項高風險模組（含會改寫用戶 NFO 的 `nfo_updater`）離線單元測試債清償。
- **前端呈現與發現性優化**（v0.10.1～v0.10.2，feature/74/75）：進階搜尋畢業為永久常駐核心，隱形長壓手勢全面移除、改「來源膠囊」處處可見觸發挑源／換源；搜尋詳情資訊密度重排、封面正面裁切共用規則、行動裝置基礎相容（scroll trap／星空門檻／觸控 overlay）補齊。
- **開發工具鏈硬化 + 前端離線可靠性**（v0.10.5～v0.10.6，feature/78/79）：`ruff` + eslint/stylelint 進 CI 擋 PR（先前純本地、沒人本地跑就漏）；GSAP/Alpine/圖示字型改本機載入斷網仍可用、前端錯誤自動記錄；`requirements.txt` 精確鎖版並把 Starlette 鎖至最新修補版清除已知 CVE。
- **MPA 跨頁轉場 + Fluent 材質統一**（v0.10.3～v0.10.4，feature/76/77）：純 CSS View Transitions 讓 sidebar 切頁白屏閃爍改平滑淡換（Showcase 因常駐動畫維持硬切）；全站材質收斂成單一 token 系統的 6 角色（Mica canvas／Glass shell／panel／caption／overlay／Media frame）+ 浮動圓角玻璃 chrome 擴及 search／settings／scanner。
- **LAN 伺服器模式 + 手機體驗完整化**（v0.10.7～v0.10.11，feature/80/81/82/83/84）：一鍵把桌面 App 開放給同區網手機／平板瀏覽（dual-listener + 區網存取閘門）；手機加主畫面圖示、封面／燈箱左右滑換片、窄螢幕破版修補；Windows 系統匣關閉行為（最小化背景執行）；燈箱封面比例自適應不留白 + 行動星形爆射相似探索面板；Windows 雙擊安裝捷徑 + Help 頁一鍵更新按鈕。

測試數 4089 → 4735。

---

## 0.9.x 系列 (v0.9.0 ~ v0.9.11, 2026-05-29 ~ 2026-06-13)

- Scraper Federation + metatube HTTP 聯邦（30 provider Parts Bin + promote/demote + 無碼 staged + SSRF 驗證）、Settings 分區 IA + 資料驅動來源 schema + 進階重刮彈窗
- Active Row 拖曳順序成搜尋路由唯一真理（拔除 primary_source）；async-offload 慢 I/O 移出 event loop（NAS stat / sqlite / config / 同步 HTTP）+ config 鎖/原子寫 + AST 守衛；封面三態（skeleton/shimmer/破圖）+ Showcase console 清零
- 新增來源：JavLibrary（BETA，桌面借 PyWebView 過 Cloudflare）+ avsox 復活（轉 JSON API）+ Tokyo Hot 單字母番號修復；8 源真實番號健康金絲雀 smoke（三態 quorum 判讀）+ cov-floor 84% 流程地板
- 本地 WebP 縮圖快取（opt-in，SSD 出圖不碰 NAS + blur-up 燈箱）+ 燈箱單筆刪除（只刪 DB row）；VR 投影標籤保留 + 自動 VR tag
- 外部媒體管理器相容（Jellyfin/Emby/Kodi 四態：poster/fanart 命名 + cd1/cd2 合併 + NFO 補欄 + Scanner 識別外部封面）；dim 暗色主題色彩編碼修復；進階搜尋畢業為永久常駐核心

測試數 2937 → 4089。

## 0.8.x 系列 (v0.8.0 ~ v0.8.10, 2026-04-28 ~ 2026-05-28)

- Charter Pilot：Fluent 2 視覺語言全站統一（§1–§6 + ease/DURATION 三角色）+ Ghost-fly Lightbox 共用化 + Alpine 釘版四插件 + 全站通知中心
- 全站前端 ESM 模組化（Import Maps，巨型單檔全解體）+ lint toolchain（eslint flat config + stylelint，frontend_lint 905→450）
- 以圖搜圖 CLIP Beta 出貨後轉向純規則式相似度排序器（拔 ML 依賴、主 ZIP 271MB→43MB）
- Tag Alias 跨語言系統 + Search→Showcase pipeline 即時化（GhostFly + DB 即時 upsert）+ Onboarding Scanner-first 翻轉 + SSRF 白名單 + 女優查詢 json_each 重寫

測試數 2705 起（系列歷經 CLIP 上下架與 lint 瘦身，末值未單列）。

## 0.7.x 系列 (v0.7.0 ~ v0.7.8, 2026-04-10 ~ 2026-04-26)

- Agentic AI API 平台首發（batch-search / generate-from-ids / enrich-single / collection-sql / capabilities manifest）
- User Tags 三層整合（DB + NFO `<user_tag>` + API + Search/Showcase 雙頁 UI）
- Actress Favorite + Showcase 女優模式（actresses DB + Orchestrator 4 路並行 + 女優 Grid/Lightbox/Hero + GSAP）+ Actress Alias CRUD
- Scanner 一鍵補完（missing-check + batch-enrich SSE 分批）+ Ghost Fly 轉場 + WinFsp/rclone 掛載相容 + 劇照幽靈 URL 修正

測試數 1818 → 2705。

## 0.6.x 系列 (v0.6.0 ~ v0.6.7, 2026-03-29 ~ 2026-04-09)

- 四語系 i18n 建立（繁中/簡中/日文/英文，~477 key + `core/i18n.py` + `window.t()` + JavBus lang 連動）
- Agentic AI API 初版（batch-search / generate-from-ids / enrich-single / collection-sql / capabilities）+ OpenAI Compatible 翻譯 Provider
- Alpine.js 技術債清理（6 頁統一 `Alpine.data()` 註冊、移除 bridge.js/init.js、SearchCore 全域消除）
- Scanner Jellyfin 圖片手動觸發 + TTL 快取 + 前端動效補強（checkmark/shake + Load More stagger + alert→toast）

測試數 1366 → 1634。

## 0.5.x 系列 (v0.5.0 ~ v0.5.5, 2026-03-25 ~ 2026-03-28)

JavBus Scraper 完全重寫（移除 jvav 第三方依賴）+ Video Model 擴充（director/duration/label/series/sample_images）+ Sample Gallery 元件（extrafanart 全鏈路）+ Metadata Pipeline 補齊（NFO 讀寫全欄位）+ Maker 名稱對照表重建（雙層 JSON + shared loader）+ DMM 模糊搜尋 + 全來源欄位補齊（Jav321/AVSOX/HEYZO/FC2/D2Pass）+ 字幕檔自動偵測搬移 + Proxy direct 模式。測試數 1007 → 1366。

## 0.4.x 系列 (v0.4.0 ~ v0.4.4, 2026-03-08 ~ 2026-03-19)

GSAP 搜尋頁動畫系統（SSE 漸進搜尋 + Mini-Burst + Grid↔Detail 共享封面轉場 + Cover State Machine）+ GSAP Showcase 動效系統（Flip 排序 + stagger 分頁 + Motion Lab）+ Lightbox 重設計（glass circle overlay + metadata panel）+ Source Link 設定 + 安裝腳本升級 + 測試套件大整合（去重 + conftest 統一 + 覆蓋率補強）。測試數 731 → 1007。

## 0.3.x 系列 (v0.3.0 ~ v0.3.6, 2026-02-08 ~ 2026-02-20)

Bootstrap → DaisyUI + Alpine.js 全站遷移 + Showcase 動態化（SQLite SSR 取代靜態 iframe）+ Search Grid Mode + 女優資料卡（Graphis + JavBus 並行）+ Fluent Material Boost（Mica/Acrylic）+ GSAP 前置基礎建設 + Scraper 擴充（D2Pass/HEYZO/DMM/gfriends/Graphis）+ 路徑工具統一（uri_to_fs_path/pathToDisplay）+ UNC 路徑修正 + 版本標記覆蓋保護 + Jellyfin 圖片模式。測試數 523 → 817。

## 0.2.x 系列 (v0.2.0 ~ v0.2.4, 2026-01-22 ~ 2026-02-07)

Design System 建立（Fluent Design 2 視覺語言 + AV Card 4 變體 + Token 系統）+ macOS 支援（Alpha）+ AI 翻譯雙引擎（Ollama + Gemini）+ 多層目錄結構 + FC2/無碼搜尋 + SQLite 本地收藏庫 + Scraper 模組化架構（5 個獨立 scraper）+ 番號後綴清理修正。測試數 126 → 564。

## 0.1.x 系列 (v0.1.0 ~ v0.1.4, 2026-01-15 ~ 2026-01-17)

初始版本發布：多來源番號搜尋（JavBus/Jav321/JavDB）+ Gallery UI + 智慧搜尋 + 批次搜尋 + NFO 自動補全 + Settings（Dark Mode + Ollama 翻譯 + 輸出格式設定）+ 新手教學（Tutorial 4 步驟）+ Windows 打包 + 路徑工具統一（path_utils）+ 版本管理集中化。測試數 115 → 311。

> 完整歷史紀錄請見 [CHANGELOG_ARCHIVE.md](CHANGELOG_ARCHIVE.md)
