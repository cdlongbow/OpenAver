# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [0.16.7] - 2026-09-24

> 這一版是三件互不相干的小事：瀏覽頁的牆上也看得到收藏女優「拍這部片時幾歲」；掃描頁
> 那些只給一個數字的地方（「缺 NFO 檔案：25 部」），點下去就看得到是哪幾部、可以一鍵複製；
> caribbeancom 的片女優不再被寫成「ログイン」、系列不再被寫成「VIP動画」。

### Added

#### 🎂 瀏覽頁卡片也顯示發行時年齡

- 過去要點開燈箱才看得到的 ` (28y)`，現在影片牆上也有：
  - **眼睛打開（資訊區展開）**：女優那一行每位收藏女優的名字後面都會出現，例如
    `明里つむぎ (24y), 某某某`。
  - **眼睛關著**：只有**單人片**會在卡片底下的女優名後面出現，例如 `桜空もも (26y)`；
    兩位以上女優的片維持原樣（多人時單獨一個歲數看不出是誰的）。
- 判斷規則與燈箱逐條相同：是你收藏的女優、有生日、發行日是完整年月日，且片長不是已知的
  239 分鐘以上（片長不明照樣顯示）；舊名／改名一樣對得上。格式與燈箱一致、淡色、點不動。
- 名字和歲數不會被拆在兩行；海報模式與手機寬度下卡片底下那一欄本來就藏起來，不受影響。
- 在女優頁加入或移除收藏，回到影片牆不必重新整理，年齡就會跟著出現或消失。

#### 🔢 掃描頁的數字點得開

- 三個地方的數字變成可以點：「NFO 與封面都缺／缺 NFO 檔案／缺封面檔案」、掃描完出現的
  「NFO 欄位不全」、以及「外部媒體管理器封面缺失」。
- 點下去在數字旁邊跳出一個小清單：頂端寫這是什麼、共幾部，右邊一顆「複製」；下面每一列是
  番號（看不出番號的片顯示檔名），「NFO 欄位不全」那份會多寫它缺哪幾欄。清單很長時只在
  裡面捲動，頂端那一列不動。
- 按「複製」→ 一行一個番號進剪貼簿，跳「已複製 N 部」。瀏覽器不允許複製時會明講
  「複製失敗」，請你手動選取清單文字，不會假裝成功。
- 點外面或按 Esc 關閉，焦點回到剛剛那個數字。清單只能看，不能在裡面勾選或補資料。
- 缺 NFO／缺封面那份清單底部多一句說明：已經補過一次卻查無結果的片、以及看不出番號的片
  **不算在這個數字裡**，要處理它們請用燈箱的 ⚙「進階重新刮削」。

### Changed

- 「缺 NFO+封面檔案」改成「**NFO 與封面都缺:**」——原本的寫法讀起來像「缺 NFO 或缺封面」，
  其實是兩者都缺。同一排的單位也統一成「部」。

### Fixed

- **caribbeancom 的片，女優被寫成「ログイン」、系列被寫成「VIP動画」**（issue #193）：
  站方頁面的簡介與導覽選單也出現了「出演」「シリーズ」這兩個字，我們抓錯了位置，抓到的是
  頁首的登入連結與選單項目。現在只認頁面上真正的演員欄與系列欄；多位女優的片會全部抓到，
  頁面上沒有系列的片系列欄留空。1pondo、10musume 不受影響。
  - **已經寫錯的片不會自動修正**：「補齊 NFO 欄位」「一鍵補完」**不會**處理它——那一欄有值
    （只是值錯了），不歸「補缺」管。請在燈箱按 ⚙「進階重新刮削」，NFO 與燈箱的女優、系列
    就會換成正確的（勾著「保留目前標題」也一樣會換）。
  - 依女優名整理出來的「ログイン」資料夾**不會**自動改名，請自己改名或合併。
  - 在瀏覽頁搜尋「ログイン」可以找出受影響的片。
- 來源健康檢查把 D2Pass 拆成 1pondo／caribbeancom／10musume 三站各自判斷，而且每站都要
  抓到那部片的**正確女優**才算健康——這次的錯就是因為過去只檢查「有抓到東西」、三站又混在
  一起算，才一直沒被發現。

### 已知限制

- **本版新增的文字只有繁體中文**，en／ja／ko 待補，那三種語言的介面下會先以繁體中文顯示
  （「NFO 與封面都缺」的簡體中文已同步）。
- caribbeancom 的舊資料通道已經停用，每次查詢都會先試一次舊通道再改讀網頁，多一次網路往返；
  結果正確，只是稍慢。

## [0.16.6] - 2026-09-24

> 這一版解決兩件跟「標題」有關的事：燈箱齒輪重刮不再自動蓋掉你翻譯過或改過的片名；
> NFO 裡給 Jellyfin／Emby／Kodi 看的片名長相也可以自己設定，接得上你過去用別的工具
> 整理過的收藏。

### Added

#### 🔁 重刮可以保留目前的標題

- 燈箱齒輪 ⚙「進階重新刮削」在刮回結果後，多一個勾選「保留目前標題：「〈目前的片名〉」」。
  **出現條件**：這次要寫入的標題與目前的標題不同時（相同就不出現，沒有差別）。
- **預設勾選**。勾著時只換封面、演員、片商、標籤、日期等資料，標題維持原樣；原始標題
  仍會更新成刮回來的名字。取消勾選則與過去相同：標題整個換成刮回來的。
- 在重刮視窗把番號改成別部片時，這個勾選不會出現——舊標題屬於另一部片，保留沒有意義。
- 有開翻譯的人，中文片名終於不會因為換封面而被站方原名蓋掉；沒開翻譯、只是想換個
  來源重刮的人，也一樣保得住原本的寫法。

#### 📝 NFO 標題格式可以自訂

- 「設定」→「刮削行為」→「進階刮削設定」→「檔案命名格式」下方新增「NFO 標題格式」。
  操作方式與檔案命名格式相同：變數是可拖曳的膠囊，中間可以自由加符號與空白，下方有
  即時預覽（含一組「演員資料缺失」的範例）。
- 這一列**只決定 NFO 裡 `<title>` 的長相**（也就是媒體伺服器海報牆上看到的名字），
  不影響檔名、資料夾，也不影響 OpenAver 自己畫面上顯示的片名。
- `{num}`（番號）與 `{title}`（片名）是必備膠囊，各恰好要有一次，沒有 ✕、按刪除鍵也
  刪不掉，但可以拖曳換位置；其他變數（`{actor}`／`{actors}`／`{maker}`／`{date}`／
  `{year}`／`{month}`／`{day}`）可自由增減、任意擺放。用其他方式送出缺少或重複的格式
  （例如透過設定 API）會在存檔時被擋下並說明原因。
- **預設值 `[{num}]{title}`，與升級前逐字相同**——沒有動這個設定的人，整份 NFO 不會
  有任何變化。
- 格式套用在所有會寫 NFO 的地方：搜尋頁「整理」、定時整理、燈箱補完、齒輪重刮、掃描頁
  補完、唯讀來源產出。標題保留原本的標點與空白（不套用檔名那套字元清理），也不會被
  截斷；欄位沒有值時位置留空、不清多餘的符號（例如片子沒演員，格式含 `{actor}` 時結尾
  留一個 `-`）。
- 改了格式之後，只有**之後**寫出的 NFO 會套用新格式；已經寫好的 NFO 不會被批次改寫，
  等它下次因為補完、重刮或重新整理而被重寫時才自然跟上。
- OpenAver 自己畫面上（卡片、燈箱、搜尋、相似片）顯示的一律是乾淨的片名本體，不會
  出現「片名-演員」這種帶著附加內容的標題，格式換幾次、重寫幾次都不會疊加或截短
  真實片名。改成 `{num}-{title}-{actor}` 之後，過去用別的工具整理、標題長得像
  `番號-片名-演員` 的舊 NFO，掃描進來也認得出乾淨的片名。

### Changed

- 搜尋頁「整理」與定時整理寫出的 NFO 標題不再被截成 50 字——過去只有這兩條路會把
  NFO 標題跟著檔名一起截斷，現在 NFO 裡是完整片名，只有檔名仍照原本的長度上限截斷。

### 已知限制

- 掃描頁產生網頁讀到其他工具的 NFO、而那份 NFO 沒有番號欄位時，自訂格式下畫面會
  顯示整串未拆解的標題（與升級前相同，非本版新增的限制）。
- 唯讀來源、且在這一版之前已經匯入其他工具 NFO 的片：把格式改成含演員的自訂格式後，
  對這些片勾「保留目前標題」重刮，輸出夾裡的 NFO 標題會多疊一段演員名；原始檔案不受
  影響，不勾保留、重刮一次即可修正。
- **本版新增的文字只有繁體中文**，en／ja／ko 待補，那三種語言的介面下會先以繁體中文
  顯示。

## [0.16.5] - 2026-09-23

> 這一版把你的資料放進一個**固定、有名字的家**：設定檔、片庫資料庫、女優照片、縮圖、
> 書籤封面全部集中到 `app/output/`（開發環境是 `<repo>/output/`）。升級時自動把舊設定
> 收進來，不動任何既有資料。同一版也把「App 打不開」的那一刻，從一片空白變成看得懂的
> 訊息，並把相簿輸出目錄的「跟著資料根走」講清楚、不再讓它落在程式資料夾裡。

### Added

#### 📁 你的資料有了固定的家

- 設定檔、片庫資料庫、女優照片、縮圖、書籤封面等，全部集中在 `app/output/`
  （開發環境是 `<repo>/output/`）。**這個資料夾從這一版起是唯一需要備份的地方。**
  （若你把設定頁「輸出目錄」自訂到別的位置，那個資料夾也要一起備份——不備份的話，
  換電腦後只要在掃描頁重新按一次「產生」也能復原。）
- **升級後第一次開啟，會自動把舊的 `app/web/config.json` 安全複製過去**——原檔不刪、
  不改，也不搬動或改寫任何其他資料（片庫、封面、DB 全部原地不動）。
- 複製完成的那一刻，通知中心會跳一則「資料位置已定版」，附上你這台機器實際的路徑，
  提醒你把這個資料夾納入備份。
- **換電腦、備份，從此只要處理這一個資料夾**；更新或覆蓋安裝都不會動到它。

#### 若舊設定不見了

- 找不到舊設定但片庫還在時：片庫原樣保留，設定回到預設值，並跳通知點名要重新設定的
  四類：來源設定、跨機器路徑映射、外部服務設定、介面偏好。
- **設定檔之後被刪掉，下次開啟會自動用預設值重建**（一樣會跳通知說明）；但如果設定檔
  本身是壞的、讀不出來，OpenAver 會拒絕開啟——避免蓋掉一份也許還救得回來的設定。

#### 🚪 開不起來時會說清楚

- 桌面版：跳出一個系統訊息視窗，明講是 OpenAver 啟動失敗、詳細原因在 `debug.log`，
  不會開出一個半殘的主視窗。
- 伺服器版（Linux／命令列）：印一行固定的中文摘要，並以固定的錯誤碼結束執行。

### Changed

#### 🗂 相簿輸出目錄：留空＝跟著資料根走

- 設定頁「輸出目錄」欄位**留空時，灰字會顯示目前實際解析出來的資料根路徑**，「產生
  網頁」也跟著寫到那裡；不必自己去猜或推算路徑在哪。
- 舊的預設值 `output` 自動一次性改成留空（實際存放位置不變）；你自己填過的自訂值
  逐字保留，檔案位置不動。
- **不能再把這個目錄設在程式安裝資料夾裡**（例如 `./output`、`Output`、`app/output`
  這類寫法）——程式資料夾屬於每次更新都會被新版覆蓋的區域，不適合放你自己的東西；存檔當場就會被擋下並說明原因。
- 掃描頁按「產生」時，如果設定沒有存成功，就不會照舊拿舊的資料夾清單去產生。

### 其他

- **片商（廠牌）對照表改成隨版本出貨**：程式執行期間只讀這份表，不再自己上網學習、
  寫回本機。使用者可見的差異只在：其他來源也查不到片商時，未知的番號前綴會維持空白，
  不再由背景查詢悄悄補上；已經入庫的片不受影響。
- Linux 伺服器部署文件、Windows 發行包裡的說明文字，都同步改成指向新的資料位置。

### 已知限制

- **設定裡如果早就把相簿輸出目錄設在程式資料夾內**（例如相對路徑），這一版不會自動
  幫你改掉；下次在設定頁或掃描頁存檔時會被擋下，需要自己改到別處或清空欄位。
- **首次開啟新版的那一刻若剛好斷電或被強制關閉**，有極小機率會少收到一則「設定已
  重建」的通知，但資料本身的安全性不受影響。
- **有設定 `OPENAVER_DATA_DIR` 的進階部署**：Linux 文件裡提到的 `output/`，指的是你
  自己設定的那個資料夾，不是預設的 `app/output/`。
- **這一版新增的文字只有繁體中文**，en／ja／ko 待補，那三種語言的介面下這幾句會先以繁體中文顯示。

## [0.16.4] - 2026-09-21

> 這一版做四件事：**把送出去的網頁內容壓縮過再傳**、**把自動對焦的人臉偵測搬出主程序**、
> **讓算不動的機器自己關掉背景自動對焦並且說一聲為什麼**，以及**把那個開關交到你手上**。
>
> ---
>
> **一、網頁內容壓縮**
>
> 人在家裡（區網）感覺不到差別。人在外面走 VPN／Tailscale 連回家裡那台，第一次打開瀏覽頁
> 過去要等 15–30 秒——那段時間有八成以上是在傳同一份東西：整個片庫的清單。現在那份清單
> 傳出去之前會先壓縮，**2,101 部的片庫實測從 3.5 MB 降到 0.69 MB**（五分之一），HTML／JS／
> CSS 也一起壓。
>
> **影片、封面、縮圖、字型一個都不碰**——那些格式本身就已經壓過了，再壓一次只是白燒 CPU。
> 畫面上沒有任何新東西，也沒有新的設定可以調。
>
> ---
>
> **二、自動對焦不再讓整個 App 停住**
>
> 無碼片的封面會自動找臉、把臉留在裁切框裡——這件事本身沒變。變的是**它跑在哪裡**：
> 過去它與網頁伺服器擠在同一個程序裡搶資源，在低階 NAS（DS218）上算一張封面要 42–53 秒，
> 掃描一輪裡有 4 部無碼片時，**整個 App 會有約 170 秒完全沒有反應**——點什麼都不動，
> 而畫面上不會有任何東西告訴你它在忙。現在那段運算搬到另一個程序去，**掃描期間 App 照常能用**。
>
> 那件事本身有個代價：在算不動的機器上，背景自動對焦會每一張都放棄。**第三件事就是在處理它。**
>
> ---
>
> **三、算不動的機器不再默默放棄**
>
> 上面那個 5 秒上限，在低階 NAS 上等於每一張都放棄——而過去那件事**完全沒有聲音**：
> 封面看起來只是裁得不對，你不會知道為什麼，也不會知道該去哪裡自己處理。
> 現在連續兩張算不完，OpenAver 就認定這台機器算不動，**主動停掉背景自動對焦並跳一則通知**
> 告訴你發生什麼事、以及逃生口在哪。**被關掉的範圍包含你自己在燈箱裡打開裁切工具時的那次自動找臉**——
> 在一台已經被判定算不動的機器上，讓你空等 40 秒再找不到臉並不是服務，所以那一次也直接跳過、
> 裁切框停在預設位置，**自己拖一下就好**。
>
> ---
>
> **四、那個開關交到你手上**
>
> 第三件事有個問題：**那是我們替你做的決定，而你沒有地方可以反對。** 設定頁那一列只是一個
> 唯讀的狀態顯示，點不動。這一版把它變成一個真正的開關——你可以自己關掉（不想讓它跑），
> 也可以在被自動關掉之後自己打開（你知道這台機器其實還行）。
>
> **而且你按過之後，我們就不再覆寫它。** 包含升級到下一版：在你沒動過的情況下換版本會重新
> 給機會，但你動過之後，就一切以你的設定為準。同一版裡也是——你把它打開，它就不會再自己關上。

### Added

#### 🙈 算不動的機器會自己關掉背景自動對焦，而且會說一聲

- **連續兩張封面都在 5 秒內算不完 ⇒ 認定這台機器算不動**，停掉背景自動對焦。省下的是之後
  每一部無碼片白等的那 5 秒；封面照樣產生，只是沿用既有的裁法。
- **停掉的那一刻，側欄跳一則通知**：說明是這台機器太慢，以及想對焦的話在燈箱裡自己拖曳。
  **一次轉態只發一則**，不會每掃一片就吵你一次。
- **關掉之後，燈箱裡的自動找臉也一起跳過**——你在影片燈箱打開「封面裁切預覽」、或在女優那邊
  打開對焦裁切時，過去會先自動找一次臉；在一台已經被判定算不動的機器上，那一次會**直接跳過**
  （不讓你空等幾十秒再告訴你找不到），裁切框停在預設位置，**自己拖一下就好**。設定頁的 `?`
  與那則通知都寫著這個逃生口。
- **設定頁可以自己開關**：「設定」→「刮削行為」→ 展開「進階刮削設定」，多一列
  **「人臉自動對焦」**，旁邊的 `?` 說明它是做什麼的。詳見下面第四段。
- **你沒動過那個開關之前，升級到下一版會自動再試一次**：判定綁著版本號，換了版本重新給機會——
  萬一哪天偵測變快了，你不必自己去哪裡把它打開。（**你動過之後就不會了**，見第四段。）

#### 🎛 那一列從「看得到」變成「按得動」

- **「設定」→「刮削行為」→「進階刮削設定」→「人臉自動對焦」現在是一個開關**。關掉之後，
  掃描時的背景自動找臉與燈箱裡打開裁切工具時的那一次自動找臉**都不跑**，裁切框停在預設位置，
  自己拖一下就好。
- **你按過之後，我們就不再覆寫它**：自動判定不會再把它關掉，升級到下一版也不會把它重新打開。
  在你沒動過之前，兩件事都照舊發生。
- **旁邊的 `?` 說明改成一段話**，不再分成「開著」與「關掉」兩種版本——因為那兩句都預設了
  「是系統關的」，而你自己關掉時那句話就是假的。新的說明同時講清楚兩件事：什麼情況我們會
  幫你關掉，以及你動過之後就以你的設定為準。
- **存檔失敗時畫面會說一聲**：開關退回原本的狀態，並跳一則訊息告訴你沒存成功（過去是靜悄悄
  地彈回去，你只會以為自己沒按到）。

#### 💬 燈箱在跳過偵測時會就地說明為什麼

過去在算不動的機器上打開裁切工具，畫面只是安靜地停在預設裁切位置，**沒有任何解釋**。現在
四種情況各說各的話：

- **這台機器的自動對焦已經關掉** → 安靜地直接進手動（你自己關的，不需要我們再解釋一次）
- **這一次算太久、而且我們順手把它關掉了** → 告訴你已經幫你關掉、以及想調整位置直接拖就可以
- **這一次算太久，但設定還開著** → 告訴你這次沒算完，可以自己拖
- **偵測本身出錯了** → 沿用既有的錯誤提示（這跟「太慢」是兩回事，不會混為一談）

文案一律不評價你的硬體（不會出現「你的機器太爛」這類說法），這條由 lint 守著。

### Changed

#### 🗜 網頁內容傳出去之前先壓縮

- **壓的**：片庫清單那份資料、瀏覽頁的 HTML、JS、CSS。本機 2,101 部片庫實測，片庫清單
  **3,576,776 → 701,691 bytes**；整頁冷載（清快取、1280×720 視窗）總傳輸 1.92 MB。
- **不壓的**：影片（含拖進度條的分段下載）、封面、縮圖、書籤封面、女優裁切圖、字型。
  這些格式本來就是壓縮過的，再壓一次省不到頻寬，只會多花 CPU。
- **壓縮強度刻意選最低的那一檔，而且不開放調整。** 在一台低階 NAS（DS218）上實測：最低強度
  壓一次片庫清單要 **0.35 秒**，再往上調一級要多花 **1.0 秒** 卻只多省 0.44 MB——以一般家用
  上傳速度換算，多花的那一秒買不回來。最低強度是唯一在所有機器、所有連線速度下都不會輸的選擇。
- **大檔案的壓縮不會卡住其他請求**：超過 128 KB 的回應交給背景執行緒處理，同時間別人要縮圖
  或播影片不會被拖住。
- **播放與 seek 完全不受影響**：拖進度條時伺服器回的分段內容一個位元組都沒變。
- **掃描／搜尋的即時進度不受影響**：那些一筆一筆推送的事件不壓縮，不會被攢成一大包才吐出來。
- **既有的「沒變動就不重傳」機制照舊**（v0.15.14 起）：第二次以後打開瀏覽頁仍然是 0 byte。
  這一版治的是**第一次**，以及任何一次資料變動之後的第一次（重開 App、更新版本、掃到新片、
  改標籤、拖對焦……）。

#### 🧍 自動對焦搬出主程序

- **掃描時 App 不再被人臉偵測卡住**。那段運算改到另一個程序去跑，掃描進行中你照樣可以
  翻牆、開燈箱、改標籤。
- **背景自動對焦有 5 秒上限**：算不完就放棄這一張，改用既有的裁法（與有碼片、以及真的
  找不到臉的片走同一條路）。**海報照樣會產生，只是沒有對到臉**。
- **放棄的那一張不會被記成「試過了」**——下一次掃描還會再試一次，不會被永久跳過。
- **你自己按「重新偵測」也有 5 秒上限**（影片燈箱與女優編輯頁兩處都是）。人站在畫面前等的
  時候，超過 5 秒還沒算完就直接讓你手動拖——在算不動的機器上，多等的那幾十秒換不到任何東西。
  **而且這一次逾時就算數**：背景要連續兩張算不完才判定，人在畫面前那一次**逾時一次**就會
  把自動對焦關起來（你已經親眼看到它算不動了，不需要我們再確認一次）。
- **同一時間只有一張封面在做偵測**，四條會用到它的路（掃描、海報產生、影片手動對焦、
  女優手動對焦）排隊共用，不會一起搶機器。

### 已知限制

- **這一版不會讓你家的上傳變快**，只是要傳的東西變少了。實際省下多少取決於你的連線。
- **壓縮強度寫死在程式裡，設定頁看不到也改不了。** 那個數字是拿低階 NAS 的實測結果定的，
  調高在低階機器上是淨賠。
- **縮圖與封面完全不壓**，所以「一頁有幾十張圖」的那部分傳輸量跟以前一樣——降幅主要來自
  片庫清單與 HTML／JS／CSS。
- 極少數圖片來源如果把格式標錯（例如把 JPEG 標成非標準的寫法），那張圖仍可能被多壓一次。
  圖片顯示正常、內容不會壞，代價只是多花一點 CPU。
- **在算不動的機器上，背景自動對焦實質上仍然不會成功**——這一版做的是讓它**不再沈默**
  （連兩張算不完就停掉並通知），不是讓它算得完。低階 NAS（DS218）算一張要 42–53 秒、
  上限是 5 秒。**逃生口是燈箱裡手動拖曳對焦**（一直都在，拖完存檔即可）。
- **被關掉的範圍包含燈箱裡的自動找臉**，不只是掃描時的背景運算。那是刻意的（在算不動的
  機器上讓你空等幾十秒不是服務），逃生口是自己拖曳；但如果你原本期待「手動那次一定會算」，
  這一版之後不會了。
- **判定是每台機器各自的，不跨機器同步**。同一個片庫從兩台機器用，兩邊各自判斷、各自記。
- **你動過那個開關之後，這台機器就不會再自動關掉自動對焦了**——包含升級到下一版。
  在一台算不動的機器上把它打開，之後每張無碼封面掃描時仍會白等 5 秒、燈箱開裁切工具也會
  白等 5 秒，而且**不會再收到任何通知**（你已經表達過你要它開著）。要回到「讓系統自己判斷」，
  目前只能走「恢復原廠設定」。這是刻意的取捨：你的決定不該被我們默默推翻。
- **同時開著兩個分頁時，另一個分頁的那一列不會即時跟著變**。在 A 分頁切換開關，B 分頁要
  重新整理才看得到新狀態；在 B 分頁按下去會以 B 看到的舊狀態為準覆蓋過去。單人使用時要刻意
  製造才會遇到。
- **連續快速點那個開關**：兩次請求不保證照你點的順序完成，最後存進去的有機會是你**前一次**的
  選擇。這時畫面上的開關停在你最後點的那一邊，而實際生效的是另一邊——**畫面不會告訴你這件事**，
  掃描時自動對焦跑不跑會跟你看到的相反。重新整理設定頁會看到真正生效的值（開關會「跳回去」）。
  隔一下再點第二次就不會遇到。
- **要把它打開請用設定頁那個開關，不要去改設定檔**：直接改檔案只撐到下一張算不完為止
  （計數器沒有跟著歸零，**再一張**逾時就會被關回去，而且會**再通知一次**）。
  設定頁那個開關不一樣——按過就是你的決定，我們不再覆寫。
- **這一版新增的文字只有繁體中文**（設定頁那一列與它的說明、那則通知、燈箱裡的四種就地說明），
  en／ja／ko 待補，在那三種語言下會顯示英文 key 的 fallback。
- **掃描時每部無碼片最多多花 5 秒**（算不完才放棄）。與改動前相比仍然快得多，而且那幾秒
  不會再卡住畫面。
- **封面放在 NAS／網路磁碟上而中途斷線時**，極端情況下那個子程序會卡住收不回來，四條
  對焦路徑會一起停擺到重開 App 為止。這是「同時只做一張」的已接受代價。
- **出貨佈局只驗過兩種**：Linux 開發環境，以及 Windows 封裝版（實測子程序能正確啟動）。
  macOS 與 Synology 套件版的子程序啟動尚未在真機上驗過。

## [0.16.3] - 2026-09-19

> 這一版修**兩個你在媒體伺服器口味（Jellyfin／Emby／Kodi）下才會踩到的洞**——都只影響
> 「已經整理好、OpenAver 只讀不寫」的那些片。
>
> 一個是：用放大鏡幫這種片補資料、標題剛好有變 → 以前那部片在 Jellyfin 牆上會變成沒圖的
> 孤兒；現在封面檔名跟著新標題一起改，圖還在。另一個是：叫 AI 改這種片的欄位、但沒帶
> 簡介／評分／來源網址 → 以前那三欄會被清空；現在維持原樣。
>
> 同一版還把產出這些檔案的程式碼拆成三個檔，**那部分零行為變更**（見下方 Changed）。

### Fixed

#### 🖼 唯讀片改了標題，封面不再變成孤兒

- 情境：媒體伺服器口味下，對一部唯讀片按放大鏡（或 AI／齒輪重刮），這一輪標題變了。
- 以前：NFO 跟 `.strm` 用新標題命名，封面還留在舊檔名 → Jellyfin／Emby／Kodi 配不到圖。
- 現在：`-poster`／`-fanart`（舊佈局還有那張同名封面）整組跟著改到新基底，舊名字零殘留；
  圖的內容逐位元組不變；你手動拖過的對焦位置也不會被重置。

#### 📝 AI 幫唯讀片改欄位，沒帶的簡介／評分／來源網址不再被清空

- 情境：AI（或你自己）送一包資料給唯讀片重刮，但這一包裡沒有簡介、評分、或來源網址。
- 以前：缺席的那幾欄會被寫成空的。
- 現在：缺席的欄位從既有 NFO 讀回來再寫出去；有帶的才以這次送的為準（空字串／`null`
  的清空契約與非唯讀那條路相同）。

### Changed

#### 🧱 唯讀來源產出程式碼拆成三個檔（零行為變更）

- 路徑命名（輸出夾在哪、每部片的資料夾叫什麼、檔名基底怎麼算）與資產寫入
  （NFO／封面／海報／`.strm`／劇照怎麼落地、舊檔怎麼清）各自獨立成檔。
- **驗收方式**：拆分前先把「產生一次唯讀來源」的完整產出凍結起來（七種情境 ×
  檔案 sha256 ＋ 資料庫欄位），拆完之後重跑同樣七種情境逐欄位比對，七組全部相同。
- 為什麼現在做：下一版要修的兩個問題（改標題後封面變孤兒、帶 AI 資料重刮會清空
  簡介）都住在這兩塊裡，而其中一個在原本的結構下**修不進去**。

### 已知限制

- **不主動掃全庫回收既有孤兒**——升級前已經踩過舊 bug、封面還留在舊檔名的片，不會自動
  被掃一遍修好。逃生口：對那一部再按一次放大鏡（這一輪標題不用再變）。
- **升級前已被清空簡介的片不回溯**——那三欄如果在舊版就被寫空了，這一版不會把它們
  變回來；要的話走燈箱齒輪整包重刮。
- 這一版**沒有新增任何 UI 文字**，沒有多語系待譯項目。
- 記錄檔（`debug.log`）裡來自資產寫入那一段的紀錄，來源名稱從
  `core.readonly_producer` 變成 `core.readonly_assets`；訊息本文不變。

## [0.16.2] - 2026-09-18

> 這一版只做一件事：**瀏覽頁的封面，改成你捲到哪裡才去拿哪裡。**
>
> 過去一進瀏覽頁的新一頁，OpenAver 就替整頁的卡片搶先做圖——包括那幾十張你這輩子
> 都不會捲到的。片庫放在 NAS 或 HDD 上時，那些搶先做的圖跟你**正在看**的那幾張
> 搶同一條線路，結果是你眼前這幾張反而慢。
>
> 現在沒進畫面的卡不去拿。看過的維持快取，捲回去不會重載、不會閃。

### Changed

#### 🖼 封面進畫面才去拿，不再一進頁就整批請求

- 打開瀏覽頁的第一頁，縮圖請求數從 **60 降到 16**（1280×720 視窗）、**60 降到 25**（3840×2160 視窗）。降掉的那些就是你當下看不到的卡。
- **往下捲會提前一屏開始準備**，所以正常速度捲動時圖是已經在那裡的，不是捲到了才開始轉。
- **捲過去又捲回來不會重新載入、不會閃**；換排序讓卡片重排也不會重複請求同一張圖。
- **回訪零重新編碼**——之前生成過的縮圖照舊直接命中，不會因為這一版而重做一次。
- **牆的外觀、捲動、篩選、換頁、卡片動畫全部不變。** 整頁的卡片一樣都在頁面裡，Ctrl+F 找得到、捲動條長度不變、0.16.0 的展開動畫與進場節奏一個都沒動。

#### ⚙ 設定頁「封面縮圖快取」開啟時不再跳確認視窗

- 撥開那個開關，直接存檔，結束。過去那個「將為 {count} 部影片建立封面縮圖、約需 {min} 分鐘於背景生成、完成後會通知你」的視窗整個消失。
- **它描述的四件事在這一版之後全部不存在**——不再全量建立、沒有分鐘數、沒有背景工作、不會有完成通知。那則「開始建立封面縮圖快取」的側欄通知也一併消失。
- **關掉那一半逐字不變**：仍然會跳「將刪除已生成的封面縮圖（釋放約 {mb}MB 空間）」的確認視窗，取消就不存檔，確認才清。

### Removed

#### 🗑 拿掉「全庫縮圖預熱」整套

- 這個功能的設計是「你第一次打開縮圖快取時，背景把全庫的縮圖一次做完」。但它的唯一觸發點是**開關從關變成開**，而出廠設定裡它本來就是開的——**所以在每一台新安裝的機器上，它一次都沒有跑過**。
- 它留下的是一個會嚇人的確認視窗（宣告要花幾分鐘背景生成）和一套從來沒有產生過結果的背景工作。這一版把端點、背景工作、確認視窗、通知文案整套移除。
- **你不會失去任何已經生成的縮圖。** 既有快取檔案原樣保留、繼續使用。

### 已知限制

- **表格／清單兩種呈現預設仍然不顯示**（v0.15.2 起的既有設計，要在「設定」→「列表顯示預設」自行開啟）。這一版沒有改動那個開關。
- 預抓的距離（提前一屏）與首屏直接載入的張數都是**固定值，沒有設定可以調**。
- 這一版**沒有新增任何 UI 文字**，所以沒有多語系待譯項目。

## [0.16.1] - 2026-09-17

> 這一版做的是**燈箱上那一列女優名字**：名字後面會多一個 ` (28y)`，告訴你她在**這片發行那天**幾歲，
> 不是現在幾歲。
>
> 同一版順手處理兩個「按了也沒用」的地方——番號認不出來的片不再顯示那顆註定失敗的 🔍 補資料鈕；
> 而既沒封面、檔名也看不出番號的片，改成直接在燈箱裡告訴你還有哪一條路可以走。

### Added

#### 🎂 燈箱女優名字後面顯示「發行時幾歲」

- 在瀏覽頁點開一部片，**你收藏的女優**名字後面會多一個淡色的 ` (28y)`——那是她在**這部片發行那天**的滿歲。
- 只在四個條件同時成立時出現：**是你收藏的女優、她有生日資料、這片的發行日是完整的年月日、片長短於 239 分鐘**。任何一項不成立，那一列與以前**逐字相同**——不會出現空括號、問號或多餘的空白。
- **別名算數**：影片的 NFO 裡寫的是舊名或別名時一樣對得上（例如 `山岸あや花` 之於 `山岸逢花`）。
- **年齡點不動**：滑鼠移上去沒有連結游標，點下去不會變成篩選條件。名字本身照樣可以點。
- **片長 239 分鐘以上的片刻意不顯示**。那多半是好幾部片湊成的合輯，一個歲數說明不了整片；寧可什麼都不寫，也不要給一個看起來很肯定卻是錯的數字。代價是有一批四小時的單片（`BEAUTY VENUS` 系列那種）也跟著不顯示，這是刻意接受的取捨。

### Changed

#### 🔍 「補資料」按鈕不再出現在按了必定失敗的片上

- 燈箱與卡片上那顆 🔍，過去只要「沒封面 **或** 沒 NFO」就會出現，但它背後的補資料**沒有番號就直接失敗**。這一版加上「有番號」這個條件。
- 以一個 6,500 部的片庫換算，這顆鈕會從 797 部片收斂到 77 部——少掉的那七百多部，按下去從來就只得到一句「缺少番號」。
- **旁邊的 ⚙「進階重新刮削」不受影響**，仍然在原地，那才是這類片真正的入口。

#### 🖼 沒封面又沒番號時，燈箱會告訴你還有哪一條路

- 既沒有封面、檔名也看不出番號的片（同一個片庫裡約 373 部），過去燈箱上只有一個破圖，畫面上沒有任何地方說明還能做什麼。
- 現在那裡會多一句話：**把圖片用影片檔名命名、放在影片檔旁邊，回掃描頁按一次「產生網頁」就會收進來**。這個做法本來就一直有效，只是從來沒有寫在畫面上。

### 已知限制

- **這一版新增的文字只有繁體中文**，英／日／韓走既有的 fallback。
- 年齡的寫法、顏色與位置**沒有任何設定可以調**。
- **年齡不能點成篩選條件**（例如「只看她 20 歲以前拍的片」），這一版不做。
- **只有燈箱那一列有年齡**，卡片展開的資訊區沒有。

> 同版另含零可見差異的內部收斂：燈箱的前端程式由單一大檔拆成五個模組，並補上對應的機械守衛。

## 0.16.x 系列（早期版本）

> 本段版本的完整全文已封存於 `CHANGELOG_ARCHIVE.md`（頂部「版本索引」可查「功能 → 版號」）。

- **v0.16.0** — 瀏覽頁的牆動起來像個整體：按眼睛展開／收起資訊時卡片順順滑開、資訊淡入，封面比例全程不變，只有畫面上看得到的卡才做動畫；眼睛按鈕兩個圖示交叉淡入。影片牆／女優牆／書籤牆的進場改成整批共用固定時間預算、從畫面中心往外擴散。

## 0.15.x 系列 (v0.15.0 ~ v0.15.16, 2026-08-27 ~ 2026-09-15)

> 本系列**全部版本**的完整全文已封存於 `CHANGELOG_ARCHIVE.md`（頂部「版本索引」可查「功能 → 版號」）。

- **v0.15.0** — 鍵盤焦點鎖補齊（搜尋頁燈箱與兩頁劇照集過去完全沒有焦點鎖，Tab 會跑到看不見的背景元素上；瀏覽頁燈箱本來有擋，但劇照集一開就整頁失效），其餘為零可見差異的內部收斂：跨貢獻者撞名的機械守衛掛進 `npm run lint` ＋ 狀態地圖產生器、五份 toast 實作收斂成單一 `Alpine.store`、搜尋頁三組行內編輯共用同一道被修過四次的 identity guard、8 個說明浮層收斂成官方巢狀元件。測試數 7202（npm test 1240 → 1288）。
- **v0.15.1** — javdb 換了一條取得資料的路：改成優先走 javdb 官方 App 在用的資料通道、網頁解析留作備援，「被 javdb 擋下來」與「安裝路徑含中日韓文字」這兩種過去會讓 javdb 完全沒有結果的情況大多能查到了，封面不再有中央浮水印，並補齊片長／導演／發行商／系列／劇照五個欄位（順帶修掉「片商欄位一直寫的是發行商」與「標題混入『顯示原標題』」）；只適用輸入番號的搜尋，關鍵字搜尋維持網頁路徑。測試數 7204 → 7405（npm test 1293）。
- **v0.15.2** — 瀏覽頁的動畫成本與呈現切換：滿頁切換封面／海報過去是「凍住一秒、閃兩次、瞬間跳過去」（準備動畫 1199ms vs 動畫時長 333ms），改成只對視窗內看得到的卡做動畫後降到 93ms；側欄「瀏覽」指示燈由永遠旋轉改為進場轉一圈就停（燈箱開著時 13fps → 61fps），並新增「永不停止的動畫」白名單守衛。**唯一使用者可見的收斂**是表格／清單兩種呈現改為預設不顯示、右上角四選一下拉換成一顆直切封面↔海報的按鈕，要表格／清單得到「設定」→「列表顯示預設」自行開啟。測試數 7405 → 7408（npm test 1293 → 1330）。
- **v0.15.3** — DMM 的番號對照表改成**隨版本出貨**（164 條，離線對帳 320 筆全中）：過去那張表是每台電腦自己一片一片查出來的，換電腦／重裝就歸零，**而且它學錯時完全無聲**（本機自學值 53 條裡 21 條是錯的，用到就會把別部片的標題與女優寫進 NFO）；這一版起完全停止讀取本機那兩個檔（檔案不刪）。另含算不出來時的兩條補償查詢路徑（`NWF-237` 這類片查得到了）、DMM 的「直連」改為尊重系統代理（八個來源裡唯一硬性繞過的那一個）。測試數 7408 → 7485（npm test 維持 1330）。
- **v0.15.4** — **AI 助理可以「改好再交回來」**：過去 AI 只能叫 OpenAver 去某個站重抓、站方給什麼就是什麼；這一版起它可以一次提交一份審過的資料（封面用 A 站、標題用 B 站並翻成中文、女優名單用 C 站），OpenAver 直接寫進 NFO 與收藏庫，中間不再多打一次外站。同時加四道保護：番號對不上就擋、純文字修改預設不動封面、AI 沒帶的簡介／評分／來源網址沿用既有 NFO 不清空、內部保留字不給冒充。**畫面上沒有對應按鈕，沒在用 AI 助理的人這一版零差別。** 測試數 7485 → 7591（npm test 維持 1330）。
- **v0.15.5** — **測試誠實度盤點，對使用者零可見改動**：一條「核心程式不可用 Windows 桌面專屬碼」的檢查因為寫錯參數而**永遠通過**，改用 `import-linter` 重寫並實測驗證擋得住（它防的是照 `docs/linux-server.md` 裝在 NAS／Linux 上的人碰到伺服器起不來，而開發機與 CI 都是綠的）；另刪掉 1,144 行覆蓋率 0→0 的測試。同版抽樣量了「接線斷掉時有多少測試還是綠的」——13 條裡 6 條在三層檢查下全綠，**各補一道會擋的 lint 檢查**（瀏覽頁搜尋框打字不篩選／掃描頁拖放失效／設定頁貼上沒反應／搜尋頁破圖救援不啟動／女優卡與燈箱欄位重複顯示），每條都真的剪斷一次留下機械收據。測試數 7591 → 7549（本版刪 42 支；npm test 維持 1330）。
- **v0.15.6** — 瀏覽頁手感修復，影片檔與資料庫零改動：拖曳資料夾中途拖到視窗外才放開，那層覆蓋層不會消失而它蓋住整頁（靠「進去幾次出來幾次」計數，從視窗外拖進來根本不發結束訊號 → 改成「還在拖就每隔一下報平安」）；女優大卡的對焦拖了等於沒拖（瀏覽頁左上角那張從來不讀存好的座標，有別名的還要重整才變）；女優牆與影片牆互切畫面跳到最底；從別頁切回瀏覽頁整牆閃一下。另補上女優大卡展開後右側那塊空白（年齡／身高／罩杯／三圍一行 ＋ 標籤／出身地／事務所藥丸填滿）。
- **v0.15.7** — 「一串字到底算不算番號」這個判斷過去散在八個地方各寫各的、答案彼此不一致，這版統一成同一張表，影片檔與資料庫零改動：檔案清單手動輸入的番號不再被自己「修正」成錯的（`n0762`→`N-0762`、`200GANA-3360`→`GANA-3360` 這類真實寫法被改掉就查不到）；`200GANA-3360`／`T28-103`／`FC2-PPV-*` 這種前面帶數字的番號改走精準搜尋（過去被當女優名字查，且「缺 NFO」清單會漏掉這類片，升級後那份清單可能變長）；連同 `[]`／`【】`／`.mp4` 包裝貼進搜尋框的一般番號也認得出來。FC2 番號統一存成 `FC2-<數字>`（舊資料不自動改，要重刮才併回同一群）；拖進來查不到的檔案多了就地改番號的 ✏️ 與「使用番號進階搜尋」入口。
- **v0.15.8** — **書籤**：看到想看、還沒入手的片可以先收起來。搜尋框左邊多「搜尋結果／書籤」切換，書籤有自己的一面牆和燈箱；**封面在按下書籤的當下就存成 webp 到本機**，原站掛掉、圖被刪、沒連網書籤牆都還是完整的。已掃描入庫的書籤每次打開都會對帳、自動沉底標上「已入手」，並可一鍵整批清掉（只動書籤，片庫與影片檔完全不碰）。書籤存在一張新的表，既有資料庫零改動。已知限制：書籤燈箱在手機／平板還不能滑動換片；0.15.7 之前入庫的 FC2 影片可能不會被標成已入手（重掃即可）。測試數 pytest 7988、npm test 1431。
- **v0.15.9** — 書籤自己對帳：片掃描入庫後那筆書籤會自動移除（掃描完成／單片重刮補完／打開書籤清單三個時機），側欄通知列出番號；「已入手」角標、沉底排序與「清理已入手」按鈕隨之退場。按「加入書籤」時改由後端先查片庫，已經有的片直接拒絕寫入、卡片就地翻成播放鍵。格牆／大卡切換鈕改成問「有沒有第二張卡可以看」，`PGD-8` 這種半截番號也出得來。打開書籤清單的等待時間不再隨片庫成長（走索引，成本只跟書籤數量有關）。
- **v0.15.10** — 書籤牆和書籤燈箱補上與搜尋牆、瀏覽牆對等的動效：切進書籤／切回搜尋結果改成淡出淡入、卡片逐張安置；點卡片開燈箱時封面從那張卡的位置飛進去、關閉時飛回原本那張卡；移除書籤改成那張卡演出離開、其餘卡片滑動補位；加入書籤時封面會從按下的那張卡飛向左上角書籤鈕、落地有反饋。書籤卡加了「N 天前加入」的天數提示膠囊——滿 14 天才出現，滿 30 天轉琥珀色（預訂片不算，發售日還沒到的不會被催）；書籤燈箱補上直接移除按鈕，也補齊了手機／平板用手指左右滑動換片。測試數 pytest 8015、npm test 1524。
- **v0.15.11** — 片庫放在 NAS 或外接碟上、那個位置連不到時，過去封面牆要每張都等連線逾時才顯示破圖，畫面上不解釋原因；這版改成後端先確認哪個位置連不到，牆一次就畫完（不再送註定失敗的請求），底部狀態列直接寫出「無法存取：\\192.168.1.177」這類訊息（同一台 NAS 底下多個資料夾只算一個、只唸一次）。全部正常時 10 分鐘、有異常時 1 分鐘會重新確認一次，且要連續兩次都連不到才算數，Wi-Fi 打嗝或 NAS 正在重開機不會被誤報。測試數 pytest 8055、npm test 1534。
- **v0.15.12** — 唯讀來源（勾了「不准 OpenAver 寫進去」的資料夾）拉回跟一般掃描同一條線：過去檔名解不出番號、旁邊沒 NFO、網路也刮不到時，那部片會完全不建卡——連帶把燈箱 ⚙ 進階重刮這個唯一的修正入口一起吃掉；現在一律建卡，重刮的入口就在。番號解析、片商對照這兩張表過去唯讀與一般掃描各用一套、答案常對不上，現在改用同一張表。自訂標籤與「補完 NFO 欄位」過去會誤寫回你特意設成唯讀保護的來源資料夾，現在改寫進輸出夾，來源資料夾零寫入；那份 NFO 這次沒被更新到時畫面會明講。已入庫的唯讀片不會自動回溯修正，要按一次 ⚙ 重刮才會套用新規則。測試數 pytest 8087、npm test 1542。
- **v0.15.13** — 搜尋頁「我的最愛」旁新增「定時整理」：開啟後每 **12 小時**自動對最愛資料夾跑一次「搜尋 → 整理」，也可以按「立刻執行一次」馬上跑；你按下「我的最愛」的那一刻，背景那一輪會在做完手上那部之後停下來讓位，畫面上該按的東西不會因為排程正在跑而變灰。查無結果、或已經整理過的番號，之後不再每次都重新問過全部 8 個來源一遍。側欄通知上限由 10 則提高到 **50 則**，關閉 App、重開機也不會清空。已知限制：定時整理判斷「這部片整理過了沒」的邏輯跟手動整理相同，分集共用一份 NFO、或只有封面沒有 NFO 的片仍會被重複整理一次（結果與手動整理一致）；最愛資料夾若指到下載中而非已完成的資料夾，可能誤搬寫到一半的檔案。
- **v0.15.14** — 檔名就是番號的片（`ABC-123.mp4` 這種）用掃描頁「一鍵補完」時，女優／片商／系列／標籤／日期都補得進去、**唯獨標題永遠停在番號**（掃描先拿檔名把標題那格佔住，而補完只填空欄位），這版起會被真正刮回來的標題取代；**從別的頁面回到瀏覽頁不再重傳整個片庫**（沒變動就沿用上一份，回去幾乎是瞬間的）；**Windows 一鍵安裝的視窗不再被滑鼠點一下就凍住**（只關這個視窗自己的快速編輯模式、不寫登錄檔）；兩支安裝程式跟著系統顯示語言走（繁中／簡中／日文／英文）；瀏覽頁女優換圖時 wiki 那張候選圖不再「抓取失敗」（wiki 把縮圖網址換到另一個網域）。升級前已被寫壞的片兩顆補完鈕都不處理，要按燈箱的 ⚙「進階重新刮削」。
- **v0.15.15** — 新手引導第 3 步過去指著側欄叫你「掃完回到 Scanner，補全按鈕會自動出現」，可是你此刻就在掃描頁、那顆按鈕也還沒出現；改成在「產生網頁」按鈕正下方**直接畫一張等一下真的會出現的卡片**，並明講「已經用其他工具整理過的片會直接匯入，不重刮」。搜尋頁那四顆按鈕過去畫面上一個字說明都沒有，改成一行講一顆，最後一行**用你自己當下的命名設定算出檔名範例**；最愛資料夾不在掃描清單裡時多一句琥珀色提醒（那正是「整理成功、卻在瀏覽頁看不到」這個坑該被攔下的時機）。同版把**五個頁面的工具列、頁首與內容左緣對到同一條線**（過去 16／24／32 三種內縮各寫各的，切頁時內容會左右跳），**外觀材質一個都沒有動**；平板寬度 992–1023 不再同時出現側欄與手機頂欄，釘在頂端的工具列與設定頁頁首不再被頂欄蓋住。
- **v0.15.16** — 四件事的共同形狀是**「按鈕按下去了，畫面說成功了，但該寫出去的東西沒有寫出去」**：片庫直接放在網路共用資料夾最上層（`O:\` 或 `\\主機\共用`）時按「整理」**100% 失敗**（issue #180，寫入錨點檢查對該路徑形狀自己拋錯而一律判成不允許；換成前綴比對後防線強度不變）；掃描頁「一鍵補完」對只缺封面的片**完全空轉卻照樣算進「N 部成功」**（issue #190，現在缺封面就真的去查一次，成功數只算真的補到東西的片，補完仍只補缺的、既有值逐字不動）；搜尋頁按「整理」寫出的 `.nfo` **簡介永遠空白、評分連標籤都沒有**（資料送到瀏覽器時那兩欄被剝掉，而整理優先採用瀏覽器送回的那份），並補一道型別防護讓非數字評分不再在**檔案已搬完之後**才讓整理崩掉。第四件是把「新片入庫問幾家來源」講定：**新片入庫問第一家有結果的來源、整包拿；既有片補資料才問多家**——三條入庫路過去有一條走的是別的方式，順手堵掉定時整理對殘缺番號做模糊搜尋後**把影片檔搬進別部片資料夾**的縫。升級前寫出的 `.nfo` 不會自動補上簡介與評分，要按 ⚙「進階重新刮削」。


## 0.14.x 系列 (v0.14.0 ~ v0.14.10, 2026-08-19 ~ 2026-08-25)

- **拿不到的時候畫面上要有話說 ＋ Windows 啟動與安裝修復**（v0.14.0）：燈箱原圖載不到、瀏覽器播放失敗改為就地顯示提示（過去是一片黑／看起來一切正常）；一般身分安裝的 WebView2 認得出來、提示視窗改用系統訊息視窗、log 不再把「視窗沒顯示」記成「用戶取消」、網址自動進剪貼簿、安裝視窗不再閃退；罩杯排序／篩選支援 L 以上。
- **檔名版本後綴自動變成標籤 ＋ 封面屬性標籤**（v0.14.1）：檔名帶 `-C`／`-U`／`-UC`／`-leak`／`-4K`／`-VR` 的片會被認出來寫成「中文字幕」「無碼破解」「無碼流出」「4K」「VR」標籤，整理／掃描／補齊／唯讀來源四條寫入路徑一致；瀏覽頁標籤篩選同時比對手貼的自訂標籤；封面左上角可顯示屬性標籤（預設關閉的 opt-in、最多 3 顆、可逐項關且不動標籤本身）。
- **分集片合併呈現 ＋ 批次整理撞名標記**（v0.14.2）：同一資料夾裡 `-cd1`／`-cd2` 的分集片在瀏覽頁合成一張卡、封面標出段別、瀏覽器播放自動接續下一段、「從收藏移除」整組連動（磁碟檔案一個都不刪）；批次整理撞名改為列表上的常駐警示標記（點開看撞到誰），不再只給一個 4 秒 toast。另修「同片兩種容器被誤認成兩段」與「整組刪除後其餘各段縮圖快取沒清乾淨」。
- **一鍵精選 ＋ 冷門女優也能收藏**（v0.14.3）：燈箱標題前多一顆星，點一下就是精選；番號前的那顆星在四種看片方式裡都跟著，漏斗選單新增「只看精選」可與其他條件疊加且會被記住，分集片一次點兩段共用，也開放給 AI 助理批次呼叫。另：查不到資料的冷門／早年女優，只要片庫裡已經有她的片就能收藏——用片封面右側自動裁一張當頭像，之後可換圖／上傳／調對焦。
- **發售日可點成條件 ＋ 女優牆眼睛鈕**（v0.14.4）：發售日跟片商、系列一樣可點出條件，並可切成等於／小於等於／大於等於或自訂年月區間；女優牆補上與影片牆同一顆眼睛，整面牆一起攤開身高／罩杯／三圍且數字可點成條件；燈箱換片箭頭改對齊封面並卸掉兩顆玻璃圓盤（修好手機上想點星星加精選、畫面卻跳到上一片）；窄螢幕女優卡名字被數字擠掉的問題修復；封面對焦拖曳中不再被誤判成換片。
- **第三方套件與來源紀錄補齊**（v0.14.5）：對使用者零可見改動；`web/static/vendor/` 底下每個檔案都查得到版本與授權，第三方來源集中記在 `docs/THIRD_PARTY.md`，並加守衛擋「換了套件卻忘了更新紀錄」。
- **metatube 來源的圖片路徑修復**（v0.14.6）：劇照不再因為自家網域白名單漏收冷門圖床而整排 403 破圖（改走你自己那台 metatube 取回，封面在 v0.13.6 已修）；抓不到原始圖床時整理／補齊／唯讀來源三條路都會自動改走 metatube，**存檔一律優先原始網址**（代理那條會重壓 +3%）；metatube 掛掉時封面仍出得來。起點是外部貢獻者 [@sheepzacks](https://github.com/sheepzacks) 的 [PR #151](https://github.com/slive777/OpenAver/pull/151)。
- **補劇照不再覆蓋你自己放的圖**（v0.14.7）：`extrafanart/` 資料夾裡已經存在的圖一律跳過、只補缺的那幾張，不管是手動放的、Jellyfin 留下的還是上次抓好的；單片／批次／補劇照三個入口一體適用。
- **瀏覽器裡選得到資料夾 ＋ Linux 伺服器部署文件**（v0.14.8）：瀏覽器下「加檔案」「加資料夾」（搜尋頁）、「輸出資料夾」（設定頁）、「加來源」（掃描頁）四個過去只會跳「需要桌面應用程式」的死路，改成開一個真的資料夾選擇視窗（記得上次位置、三個入口各記各的、起點在你第一個掃描來源的上一層）；把資料夾拖進掃描頁不再靜默失敗；播不出來時分「格式不支援」與「檔案連不到」兩則文案；新增 `docs/linux-server.md`（headless 安裝／升級／密碼重設／systemd 選配，附未在 NAS 驗證的 Docker 實驗性範例）。
- **清除鈕與搜尋狀態不再跨分頁互相干擾**（v0.14.9）：女優牆與影片牆的清除鈕（✕）過去共用同一份狀態，在其中一頁按下去會把另一頁的搜尋字與條件標籤也清掉；女優分頁打的字重整後也會消失、精準命中的最愛女優大卡切頁後跟著不見——這一版全部修正為各分頁獨立。另外把搜尋列在不同桌機寬度下的尺寸對齊、手機上兩枚條件標籤不再撐成三行、兩頁的拖放提示層外觀統一。
- **開著代理軟體時不再一啟動就崩潰 ＋ metatube 來源抖一下不會永久消失**（v0.14.10）：App 啟動時「問自己伺服器好了沒」這句健康檢查過去會被送去你的代理軟體，代理轉不到還在啟動中的伺服器就直接崩潰；現在這句話不再走代理，啟動失敗時也會告訴你實際用的端口號並附上 `debug.log` 完整路徑。另外修好某個 metatube 來源只要逾時一次就被永久踢出自動搜尋的問題，改成冷卻幾分鐘後自動重試。

## 0.13.x 系列 (v0.13.0 ~ v0.13.14, 2026-08-01 ~ 2026-08-15)

- **封面正典位置與媒體伺服器整合收斂**（v0.13.0、v0.13.2～v0.13.4，feature/109 起）：唯讀來源封面產出邏輯先收斂成單一入口（v0.13.0，純內部、零行為變更）；修好「補齊 Jellyfin 圖片」把只有 `-poster` 的片封面原圖就地裁小且不可還原、以及封面被記成 `-fanart` 誤判成缺 NFO 兩個問題（v0.13.3）；接著把封面「正典位置」翻面——不再多產一張同名封面、`-fanart` 升格為正典，修好接 Jellyfin／Emby／Kodi 時封面牆橫式圖塞進直式格子（v0.13.4）；外部媒體管理器設「關閉」時唯讀來源不再多產兩張沒人看的圖、省空間，同時修好 HEYZO 站方改版造成的搜尋整個查無問題（v0.13.2）。
- **第三方 NFO 誤判缺漏遭覆寫修復**（v0.13.5）：片庫裡若有 Jellyfin／Emby／其他刮削器留下的 `.nfo`，裡面明明有的演員／片商／發行日／分類標籤過去會被誤判缺漏、上網刮一份回來覆寫掉——四種讀不到的寫法全部修好；另修「欄位有標籤沒內容當成已有資料」「全空白標籤吃掉後面合法值」「補完寫完檔案卻整片報失敗」「同一女優兩個下載互相覆蓋」等靜默 bug。
- **區網與帳密安全性全面強化**（v0.13.1、v0.13.6～v0.13.8）：升級元件消除 17 個已知 CVE、整理與唯讀庫產出加上路徑逃逸最終防線（v0.13.1，feature/110）；metatube 帳密三處外流出口封閉、圖片來源改宣告式白名單（v0.13.6）；區網連線可設 4 位密碼，擋下的人看到偽裝頁而非登入畫面，連錯自動鎖定並加公網暴露警告（v0.13.7）；密碼保護開啟後可給 AI agent 專屬連線 token（改密碼即自動撤換），設定頁的 API 金鑰／metatube token 不再完整送進瀏覽器、只回後四碼（v0.13.8）。
- **瀏覽篩選與女優牆體驗升級**（v0.13.9～v0.13.11）：瀏覽頁篩選從「點一下就整段蓋掉關鍵字」改成可疊加的條件標籤（女優／標籤／片商／導演／系列／廠牌可同時掛上、取交集，v0.13.9）；女優燈箱的年齡／身高／罩杯也能點出條件，並可改成以下／以上／自訂區間（v0.13.10）；女優牆新增「從片庫加入女優」面板，依片數由多到少排序、一鍵收藏庫內尚未加入最愛的人（v0.13.11）。
- **FC2 來源修復**（v0.13.12～v0.13.13）：FC2 來源鏡像站整站被 Cloudflare 擋到搜尋 100% 查無資料，改打官方站恢復搜尋，並讓劇照支援的檔案類型對齊 Jellyfin、修好部分 Windows 上側邊欄 logo 顯示成破圖（v0.13.12）；官方站已下架的片再補一條備援來源 FC2-javten（僅限 Windows 桌面版、重刮彈窗手動選用，v0.13.13）。
- **桌面影片牆新增直式海報卡型**（v0.13.14）：桌面工具列的模式選單多一條「直式海報」，與「完整封面」並列可切換、切換即時記住；只是同一張封面在畫面上的裁切顯示，不動封面原檔、不碰 NFO。

測試數 5640 → 6728（pytest；npm test 287 → 825；v0.13.13 全文未附測試段落，範圍取有紀錄的相鄰版本）。

## 0.12.x 系列 (v0.12.0 ~ v0.12.10, 2026-07-14 ~ 2026-07-24)

- **焦點裁切上線與收尾**（v0.12.0～v0.12.1、v0.12.3，feature/98/99/101）：無碼封面（FC2／素人／uncensored）刮削或掃描時自動偵測人臉、以臉為中心裁切封面與燈箱大圖，有碼片維持原樣零成本；燈箱補上「自動對焦→左右拖曳微調→存檔」正式互動、唯讀來源也納入自動對焦、牆上小格裁切改與燈箱大圖同精準度；後續再擴及 Jellyfin/Emby/Kodi 媒體伺服器海報，對焦等待動畫改視覺連續交棒，影片對焦鈕改為只在窄螢幕／手機才出現。
- **女優照片管理更完整**（v0.12.2、v0.12.4，feature/100/102）：女優頭像可自行上傳照片、也能像影片一樣手動對焦裁切；修好改過名（有別名）的女優在照片挑選視窗選本機候選必失敗、雲端重抓永遠查不到圖的問題，重抓改為自動輪流用她的每個名字查詢；順手讓搜尋詳情、燈箱、劇照瀏覽都能用滑鼠滾輪切換上一片／下一片。
- **技術債清償**（v0.12.5，feature/103）：約 130 行硬編碼中文文字收進語言檔；拖曳檔案與貼上檔名的番號辨識規則統一為同一套後端規則（不再兩套邏輯漂移）、辨識失敗改明確提示；清除死碼、logger 與中文硬編碼機械 lint 守衛正式上線。
- **唯讀來源（雲端／NAS 分享盤）功能大幅強化**（v0.12.6、v0.12.7，feature/104/105）：已經整理好的唯讀庫（旁邊有 `.nfo`／封面）改為直接就地讀用、不再無腦重刮，唯讀片的放大鏡補料／齒輪重刮／補劇照三顆鈕全面解禁（產物仍一律落獨立輸出夾、來源零寫入）；並把散在四處、常常改一處漏改一處的「記帳與回報」邏輯收斂成一份共用實作，順手修好唯讀破圖、原文標題被清空等一批連帶 bug。
- **整理前手動修正 + 桌面體驗收尾**（v0.12.8～v0.12.10，feature/106/107/108）：拖檔進搜尋頁後可在整理前直接改演員名單、幫無碼片補發售日；桌面版加上開機自動檢查更新（可關）、修好深色主題幾顆看不清的按鈕；手機瀏覽封面吃滿寬度、觸控裝置封面不再擠一排操作圖示（改進燈箱操作）、女優牆卡片大小對齊影片牆不再忽大忽小。

測試數 5233 → 5640（0.12.0 全文未附測試段落，範圍取最早可查的 0.12.1 起；前一系列 0.11.12 收尾值為 5030）。

## 0.11.x 系列 (v0.11.0 ~ v0.11.12, 2026-06-27 ~ 2026-07-12)

- **v0.11.0**：JavBus 過度泛用清償 + exact 番號搜尋改優先序 cascade（feature/85）——直接搜番號改為依你拖曳的來源優先順序逐一查詢（cascade，命中即回），JavBus 不再無視優先序搶先短路；DMM proxy 透傳修復、前綴搜尋 `type` 參數修正；拔除已死的 JavBus variant（同番號多版本）探查死碼。
- **v0.11.1**：JavLibrary 同番號多版本手動切換（feature/86）——搜尋框直搜／燈箱換來源／結果卡替換來源三入口皆可看封面手動挑撞號版本（游標預設停最新發行日），桌面 standalone 限定（需 CF transport）。
- **v0.11.2**：`core/database.py` 模組化拆分（feature/87）——2,152 行單檔拆成 `core/database/` 套件（六個領域子模組 + 永久 re-export facade），消除 `AliasRepository`／`TagAliasRepository` 鏡像重複碼（共用泛型基類），零行為／API／schema 變更。
- **v0.11.3**：唯讀來源生成本地媒體庫「off 風味」首發（feature/88）——scanner 來源可勾「唯讀」＋設輸出夾，來源零寫入下生成每片一資料夾的本地庫（NFO + 封面 + 劇照）並直接寫進 DB，供 OpenAver 自身瀏覽／串流播放雲端原檔；給 Emby/Jellyfin/Kodi 的 media-server（`.strm`）風味延後至下一版（feature/89）。

- **v0.11.4**：唯讀產生庫「地基」+ 掃描頁「試過」記憶 + 來源刪檔清死卡（feature/89）——生成片記住輸出夾（`videos.output_dir`）重刮原地更新不長重複夾、off 風味固定輸出夾免設定；試過／已生成的片不再被「缺資料」嘮叨、刮不到只試一次；唯讀網盤掉線明確警告不誤報成功；來源刪檔後 DB-row-only 清死卡（零檔案刪除）。
- **v0.11.5**：唯讀來源生成媒體伺服器庫「.strm 風味」+ 跨機器路徑映射 + 唯讀寫入全面封鎖（feature/90）——唯讀來源可產出 `.strm` 捷徑檔給 Emby/Jellyfin/Kodi 掃描播放，跨機器「播放端路徑替換」規則讓 `.strm` 內路徑翻成播放端看得懂的形式、改規則一鍵同步改寫既有 `.strm`；同時把唯讀來源「零寫入」補到滴水不漏（勾唯讀破壞性確認、四個寫入入口全面停用、切模式清舊媒體卡、產生中斷乾淨收尾）。
- **v0.11.6**：跨機器路徑映射（WSL2+UNC）讀寫全棧收斂 + DB-key 命名空間守衛（feature/91）——純 correctness 重構：修好「讀取端忘了在碰磁碟前把映射路徑反解回本機路徑」導致縮圖／封面／串流跨機器讀不到的一類 silent bug，以及「已反解路徑又被裸餵回 DB key」導致重刮掉使用者標籤、女優照 403 的另一類；兩支 AST 結構守衛擋死回歸。

- **v0.11.7**：搜尋頁體驗優化（feature/92）——搜尋列右側控制項不再擠壓變形、整理入口常駐可見更好找、整理入庫「飛入側邊欄」動畫加強為三段節奏，並抽成可跨頁複用的共用元件；順手修好「重新整理已有結果頁面時出現兩組 ✕」的 latent bug。
- **v0.11.8**：各刮削來源評分/簡介補進 NFO（feature/93，NFO-only）——七源評分、六源簡介寫進 `<rating>`/`<plot>`，純服務媒體伺服器（Jellyfin/Emby/Kodi）用戶，OpenAver 自身介面不顯示；順手美化燈箱中繼資訊視覺層次。
- **v0.11.9**：掃描頁「補資料」升級成逐片進度卡 + 命中封面飛入圖書館（feature/94）——補資料時逐片可視進度（搜尋中／命中／查無／失敗／唯讀跳過），真封面命中飛入側邊欄「瀏覽」入口。
- **v0.11.10**：設定頁命名區膠囊化 + 列表生成兩層 IA 重排（feature/95）——檔案命名格式從手打字串改成「變數膠囊 + 字面文字」視覺化編輯器、資料夾層級改動態清單（硬上限 3 層）；列表生成設定拆成日常常用（常駐）+ 離線 HTML 匯出（摺疊進階）。
- **v0.11.11**：前端靜態守衛 pytest → lint 全面遷移（feature/96，test-deflation）——純內部工程里程碑（零產品碼、對使用者隱形）：把 `test_frontend_lint.py` 裡「讀原始碼做字串/結構斷言」的守衛搬回工具層（新增 `i18n_lint`／`static_guard_lint`／`css-guard` 三支 `.mjs` + eslint `SEL_*` 家族），`test_frontend_lint.py` 16,749→5,041 行（−70%）、36 個真 contract class relocate 進 `frontend_contracts/`；north-star＝「能用 lint 機械處理的不進 pytest、不耗 Codex 審」。
- **v0.11.12**：javdb 在 released 版復活（feature/97）——打包瘦身刪除所有 `.dist-info` 導致 curl_cffi import-time 拋 `PackageNotFoundError`、被靜默吞掉，released Windows/macOS 版 javdb 從第一天就零 HTTP 請求；不再剝除 dist-info 即修復，並補打包產物 runtime 驗證守衛（`verify_artifact_imports.py` + dist-info 靜態檢查 + CI verify job）堵死 dev-only 測試盲區；本版另 bundle 兩個後續修復：非 ASCII 安裝路徑 `curl error 77`（CAINFO 改用 ANSI code page 編碼）+ 番號 7 字母前綴拖檔截斷修復。

測試數 4735 → 5523（v0.11.10）→ 4985（v0.11.11 test-deflation：守衛遷 lint 層等價承接，−538 非覆蓋損失）→ 5030（v0.11.12，+45：打包產物驗證守衛 + javdb CAINFO + 番號 cap 對齊回歸鎖）。

## 0.10.x 系列 (v0.10.0 ~ v0.10.11, 2026-06-18 ~ 2026-06-24)

- **來源穩定性 + 測試硬化**（v0.10.0，feature/73）：8 源真實番號健康金絲雀 smoke（三態 quorum 判讀）+ avsox 復活（站方轉 SPA 後改打背後 JSON API）+ Tokyo Hot 單字母無碼番號查無修復 + 覆蓋率地板 84%（`cov-floor`）+ 五項高風險模組（含會改寫用戶 NFO 的 `nfo_updater`）離線單元測試債清償。
- **前端呈現與發現性優化**（v0.10.1～v0.10.2，feature/74/75）：進階搜尋畢業為永久常駐核心，隱形長壓手勢全面移除、改「來源膠囊」處處可見觸發挑源／換源；搜尋詳情資訊密度重排、封面正面裁切共用規則、行動裝置基礎相容（scroll trap／星空門檻／觸控 overlay）補齊。
- **開發工具鏈硬化 + 前端離線可靠性**（v0.10.5～v0.10.6，feature/78/79）：`ruff` + eslint/stylelint 進 CI 擋 PR（先前純本地、沒人本地跑就漏）；GSAP/Alpine/圖示字型改本機載入斷網仍可用、前端錯誤自動記錄；`requirements.txt` 精確鎖版並把 Starlette 鎖至最新修補版清除已知 CVE。
- **MPA 跨頁轉場 + Fluent 材質統一**（v0.10.3～v0.10.4，feature/76/77）：純 CSS View Transitions 讓 sidebar 切頁白屏閃爍改平滑淡換（Showcase 因常駐動畫維持硬切）；全站材質收斂成單一 token 系統的 6 角色（Mica canvas／Glass shell／panel／caption／overlay／Media frame）+ 浮動圓角玻璃 chrome 擴及 search／settings／scanner。
- **LAN 伺服器模式 + 手機體驗完整化**（v0.10.7～v0.10.11，feature/80/81/82/83/84）：一鍵把桌面 App 開放給同區網手機／平板瀏覽（dual-listener + 區網存取閘門）；手機加主畫面圖示、封面／燈箱左右滑換片、窄螢幕破版修補；Windows 系統匣關閉行為（最小化背景執行）；燈箱封面比例自適應不留白 + 行動星形爆射相似探索面板；Windows 雙擊安裝捷徑 + Help 頁一鍵更新按鈕。

測試數 4089 → 4735。

---

## 0.9.x 系列 (v0.9.0 ~ v0.9.11, 2026-05-29 ~ 2026-06-13)

- Scraper Federation + metatube HTTP 聯邦（30 provider Parts Bin + promote/demote + 無碼 staged + SSRF 驗證）、Settings 分區 IA + 資料驅動來源 schema + 進階重刮彈窗
- Active Row 拖曳順序成搜尋路由唯一真理（拔除 primary_source）；async-offload 慢 I/O 移出 event loop（NAS stat / sqlite / config / 同步 HTTP）+ config 鎖/原子寫 + AST 守衛；封面三態（skeleton/shimmer/破圖）+ Showcase console 清零
- 新增來源：JavLibrary（BETA，桌面借 PyWebView 過 Cloudflare）+ avsox 復活（轉 JSON API）+ Tokyo Hot 單字母番號修復；8 源真實番號健康金絲雀 smoke（三態 quorum 判讀）+ cov-floor 84% 流程地板
- 本地 WebP 縮圖快取（opt-in，SSD 出圖不碰 NAS + blur-up 燈箱）+ 燈箱單筆刪除（只刪 DB row）；VR 投影標籤保留 + 自動 VR tag
- 外部媒體管理器相容（Jellyfin/Emby/Kodi 四態：poster/fanart 命名 + cd1/cd2 合併 + NFO 補欄 + Scanner 識別外部封面）；dim 暗色主題色彩編碼修復；進階搜尋畢業為永久常駐核心

測試數 2937 → 4089。

## 0.8.x 系列 (v0.8.0 ~ v0.8.10, 2026-04-28 ~ 2026-05-28)

- Charter Pilot：Fluent 2 視覺語言全站統一（§1–§6 + ease/DURATION 三角色）+ Ghost-fly Lightbox 共用化 + Alpine 釘版四插件 + 全站通知中心
- 全站前端 ESM 模組化（Import Maps，巨型單檔全解體）+ lint toolchain（eslint flat config + stylelint，frontend_lint 905→450）
- 以圖搜圖 CLIP Beta 出貨後轉向純規則式相似度排序器（拔 ML 依賴、主 ZIP 271MB→43MB）
- Tag Alias 跨語言系統 + Search→Showcase pipeline 即時化（GhostFly + DB 即時 upsert）+ Onboarding Scanner-first 翻轉 + SSRF 白名單 + 女優查詢 json_each 重寫

測試數 2705 起（系列歷經 CLIP 上下架與 lint 瘦身，末值未單列）。

## 0.7.x 系列 (v0.7.0 ~ v0.7.8, 2026-04-10 ~ 2026-04-26)

- Agentic AI API 平台首發（batch-search / generate-from-ids / enrich-single / collection-sql / capabilities manifest）
- User Tags 三層整合（DB + NFO `<user_tag>` + API + Search/Showcase 雙頁 UI）
- Actress Favorite + Showcase 女優模式（actresses DB + Orchestrator 4 路並行 + 女優 Grid/Lightbox/Hero + GSAP）+ Actress Alias CRUD
- Scanner 一鍵補完（missing-check + batch-enrich SSE 分批）+ Ghost Fly 轉場 + WinFsp/rclone 掛載相容 + 劇照幽靈 URL 修正

測試數 1818 → 2705。

## 0.6.x 系列 (v0.6.0 ~ v0.6.7, 2026-03-29 ~ 2026-04-09)

- 四語系 i18n 建立（繁中/簡中/日文/英文，~477 key + `core/i18n.py` + `window.t()` + JavBus lang 連動）
- Agentic AI API 初版（batch-search / generate-from-ids / enrich-single / collection-sql / capabilities）+ OpenAI Compatible 翻譯 Provider
- Alpine.js 技術債清理（6 頁統一 `Alpine.data()` 註冊、移除 bridge.js/init.js、SearchCore 全域消除）
- Scanner Jellyfin 圖片手動觸發 + TTL 快取 + 前端動效補強（checkmark/shake + Load More stagger + alert→toast）

測試數 1366 → 1634。

## 0.5.x 系列 (v0.5.0 ~ v0.5.5, 2026-03-25 ~ 2026-03-28)

JavBus Scraper 完全重寫（移除 jvav 第三方依賴）+ Video Model 擴充（director/duration/label/series/sample_images）+ Sample Gallery 元件（extrafanart 全鏈路）+ Metadata Pipeline 補齊（NFO 讀寫全欄位）+ Maker 名稱對照表重建（雙層 JSON + shared loader）+ DMM 模糊搜尋 + 全來源欄位補齊（Jav321/AVSOX/HEYZO/FC2/D2Pass）+ 字幕檔自動偵測搬移 + Proxy direct 模式。測試數 1007 → 1366。

## 0.4.x 系列 (v0.4.0 ~ v0.4.4, 2026-03-08 ~ 2026-03-19)

GSAP 搜尋頁動畫系統（SSE 漸進搜尋 + Mini-Burst + Grid↔Detail 共享封面轉場 + Cover State Machine）+ GSAP Showcase 動效系統（Flip 排序 + stagger 分頁 + Motion Lab）+ Lightbox 重設計（glass circle overlay + metadata panel）+ Source Link 設定 + 安裝腳本升級 + 測試套件大整合（去重 + conftest 統一 + 覆蓋率補強）。測試數 731 → 1007。

## 0.3.x 系列 (v0.3.0 ~ v0.3.6, 2026-02-08 ~ 2026-02-20)

Bootstrap → DaisyUI + Alpine.js 全站遷移 + Showcase 動態化（SQLite SSR 取代靜態 iframe）+ Search Grid Mode + 女優資料卡（Graphis + JavBus 並行）+ Fluent Material Boost（Mica/Acrylic）+ GSAP 前置基礎建設 + Scraper 擴充（D2Pass/HEYZO/DMM/gfriends/Graphis）+ 路徑工具統一（uri_to_fs_path/pathToDisplay）+ UNC 路徑修正 + 版本標記覆蓋保護 + Jellyfin 圖片模式。測試數 523 → 817。

## 0.2.x 系列 (v0.2.0 ~ v0.2.4, 2026-01-22 ~ 2026-02-07)

Design System 建立（Fluent Design 2 視覺語言 + AV Card 4 變體 + Token 系統）+ macOS 支援（Alpha）+ AI 翻譯雙引擎（Ollama + Gemini）+ 多層目錄結構 + FC2/無碼搜尋 + SQLite 本地收藏庫 + Scraper 模組化架構（5 個獨立 scraper）+ 番號後綴清理修正。測試數 126 → 564。

## 0.1.x 系列 (v0.1.0 ~ v0.1.4, 2026-01-15 ~ 2026-01-17)

初始版本發布：多來源番號搜尋（JavBus/Jav321/JavDB）+ Gallery UI + 智慧搜尋 + 批次搜尋 + NFO 自動補全 + Settings（Dark Mode + Ollama 翻譯 + 輸出格式設定）+ 新手教學（Tutorial 4 步驟）+ Windows 打包 + 路徑工具統一（path_utils）+ 版本管理集中化。測試數 115 → 311。

> 完整歷史紀錄請見 [CHANGELOG_ARCHIVE.md](CHANGELOG_ARCHIVE.md)
